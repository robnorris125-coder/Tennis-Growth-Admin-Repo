"use client";

import { ReactNode, useEffect, useState } from "react";
import { EmailSettingsCard } from "./EmailSettingsCard";

type View="home"|"schedule"|"customers"|"programmes"|"camps"|"coaches"|"venues"|"reports"|"invoices"|"payments"|"email"|"settings";
type Customer={id:string;player:string;payer:string;email:string;phone:string;programme:string;balance:string;payment:string;status:string;medical:string;emergency:string;consent:string;dateOfBirth:string;createdAt:string};
type Session={id:number;day:number;start:string;title:string;meta:string;venue:string;coach:string;tone:string;termId:string;duration:number;sessionDate:string};
type Term={id:string;name:string;startDate:string;endDate:string;sessionCount:number;status:string;sortOrder:number};
type Programme={id:string;name:string;type?:string;weekday:number;startTime:string;venue:string;coach:string;capacity:number;pricePence:number;tone:string;status:string;minAgeYears:number;maxAgeYears:number;suggestedNextProgrammeId:string};
type Staff={id:string;name:string;role:string;email:string;phone:string;qualifications:string;safeguarding:string;employmentStatus:string;payRatePence:number;notes:string;status:string};
type Enrolment={id:number;programmeId:string;customerId:string;termId:string;status:string;trialSessionsAllowed:number;trialSessionsCompleted:number;trialStatus:string;nextTrialSession:string};
type TermRollover={id:number;currentTermId:string;nextTermId:string;status:string;preparedAt:string;completedAt:string|null};
type RolloverDecision={id:number;rolloverId:number;enrolmentId:number;customerId:string;currentProgrammeId:string;nextProgrammeId:string;continuationStatus:string;progressionStatus:string;progressionReason:string;ageAtNextTermMonths:number|null;updatedAt:string};
type Camp={id:string;name:string;startDate:string;endDate:string;venue:string;time:string;capacity:number;dayPricePence:number;fullPricePence:number;status:string};
type CampBooking={id:number;campName:string;playerId:string;bookingOption:string;amountPence:number;paymentMethod:string;status:string;createdAt:string};
type Attendance={id:number;sessionId:number;playerId:string;mark:string;attendedOn:string};
type ProgrammeRegister={id:number;programmeId:string;termId:string;weekNumber:number;sessionDate:string;status:string;completedAt:string|null};
type ProgrammeRegisterMark={id:number;registerId:number;playerId:string;mark:string;note:string;markedAt:string};
type Invoice={id:number;invoiceNumber:string;customerId:string;programmeId:string;termId:string;amountPence:number;status:string;emailStatus:string;paymentStatus:string;pdfRef:string;sentAt:string|null;dueDate:string;note:string;createdAt:string;lifecycleStatus:"Active"|"Voided"|"Replaced";activeBillingKey:string|null;replacedByInvoiceId:number|null;voidReason:string;voidedAt:string|null};
type InvoiceItem={id:number;invoiceId:number;description:string;quantity:number;unitPence:number;totalPence:number};
type Payment={id:number;invoiceId:number|null;customerId:string;amountPence:number;method:string;status:string;reference:string;paidAt:string;notes:string};
type EmailRow={id:number;customerId:string;recipient:string;subject:string;body:string;status:string;createdAt:string};
type InvoiceEmailLog={id:number;invoiceId:number;playerId:string;payerId:string;intendedRecipient:string;actualRecipient:string;sendingAccount:string;sendStatus:"Sent"|"Failed";sentAt:string;gmailMessageId:string;pdfAttached:boolean;failureReason:string;deliveryMode:"test"|"live"};
type GmailConnection={configured:boolean;connected:boolean;email:string;testRecipient:string;deliveryMode:"test"|"live";error?:string};
type CurrentUser={email:string;displayName:string;role:string};
type Snapshot={customers:Customer[];sessions:Session[];terms:Term[];programmes:Programme[];enrolments:Enrolment[];termRollovers:TermRollover[];rolloverDecisions:RolloverDecision[];camps:Camp[];campBookings:CampBooking[];attendance:Attendance[];programmeRegisters:ProgrammeRegister[];programmeRegisterMarks:ProgrammeRegisterMark[];invoices:Invoice[];invoiceItems:InvoiceItem[];payments:Payment[];emailOutbox:EmailRow[];invoiceEmailLogs:InvoiceEmailLog[];venues:{id:string;name:string;courts:number;status:string}[];staff:Staff[];settings:{key:string;value:string}[]};

const empty:Snapshot={customers:[],sessions:[],terms:[],programmes:[],enrolments:[],termRollovers:[],rolloverDecisions:[],camps:[],campBookings:[],attendance:[],programmeRegisters:[],programmeRegisterMarks:[],invoices:[],invoiceItems:[],payments:[],emailOutbox:[],invoiceEmailLogs:[],venues:[],staff:[],settings:[]};
const nav:{id:View;label:string;icon:IconName}[]=[
  {id:"home",label:"Home",icon:"home"},
  {id:"schedule",label:"Schedule",icon:"calendar"},
  {id:"customers",label:"Customers & Players",icon:"users"},
  {id:"programmes",label:"Programmes",icon:"programmes"},
  {id:"camps",label:"Holiday Camps",icon:"sun"},
  {id:"coaches",label:"Coaches",icon:"coach"},
  {id:"venues",label:"Venues",icon:"venue"},
  {id:"invoices",label:"Invoices",icon:"invoice"},
  {id:"payments",label:"Payments",icon:"payment"},
  {id:"email",label:"Communications",icon:"message"},
  {id:"reports",label:"Reports",icon:"report"},
  {id:"settings",label:"Settings",icon:"settings"}
];
const money=(p:number)=>`£${(p/100).toFixed(2)}`;

type IconName="home"|"calendar"|"users"|"programmes"|"sun"|"coach"|"venue"|"invoice"|"payment"|"message"|"report"|"settings";
function AppIcon({name}:{name:IconName}){
  const common={fill:"none",stroke:"currentColor",strokeWidth:1.9,strokeLinecap:"round" as const,strokeLinejoin:"round" as const};
  const paths:Record<IconName,ReactNode>={
    home:<><path d="M3.5 10.5 12 3.7l8.5 6.8"/><path d="M5.8 9.2v10.2h12.4V9.2M9.5 19.4v-6.2h5v6.2"/></>,
    calendar:<><rect x="3.5" y="5.2" width="17" height="15" rx="2.2"/><path d="M7.5 3.5v3.4M16.5 3.5v3.4M3.5 9.3h17M7.3 13h2.2M12.2 13h2.2M7.3 16.8h2.2"/></>,
    users:<><circle cx="9" cy="8" r="3.2"/><path d="M3.8 19c.5-4 2.2-6 5.2-6s4.7 2 5.2 6M15.1 6.1a3 3 0 0 1 0 5.8M16.2 13.6c2.4.5 3.7 2.3 4 5.4"/></>,
    programmes:<><rect x="3.6" y="4" width="16.8" height="16" rx="2.2"/><path d="M7.5 8h9M7.5 12h9M7.5 16h5.2"/></>,
    sun:<><circle cx="12" cy="12" r="4"/><path d="M12 2.5v2M12 19.5v2M2.5 12h2M19.5 12h2M5.3 5.3l1.4 1.4M17.3 17.3l1.4 1.4M18.7 5.3l-1.4 1.4M6.7 17.3l-1.4 1.4"/></>,
    coach:<><circle cx="12" cy="7.2" r="3.2"/><path d="M5.7 20c.5-4.6 2.6-7 6.3-7s5.8 2.4 6.3 7M18.7 4.2l1.8-1.8M18.6 7.1h2.6"/></>,
    venue:<><path d="M12 21s6.3-6 6.3-11.4a6.3 6.3 0 1 0-12.6 0C5.7 15 12 21 12 21Z"/><circle cx="12" cy="9.6" r="2.2"/></>,
    invoice:<><path d="M6 3.5h9l3 3v14H6zM15 3.5v3h3M9 11h6M9 15h6"/><path d="M10.8 8.2c-.8-.6-2.4-.2-2.4.9 0 1.5 2.6.7 2.6 2.2 0 1.1-1.6 1.5-2.5.8"/></>,
    payment:<><rect x="3" y="5.5" width="18" height="13" rx="2.3"/><path d="M3 9.5h18M7 15h3"/></>,
    message:<><path d="M4 5.2h16v11.2H9l-5 4z"/><path d="M8 9h8M8 12.5h5"/></>,
    report:<><path d="M5 20V9M12 20V4M19 20v-7"/><path d="M3 20.5h18"/></>,
    settings:<><circle cx="12" cy="12" r="3"/><path d="M12 2.8v2.1M12 19.1v2.1M2.8 12h2.1M19.1 12h2.1M5.5 5.5 7 7M17 17l1.5 1.5M18.5 5.5 17 7M7 17l-1.5 1.5"/></>
  };
  return <svg viewBox="0 0 24 24" aria-hidden="true" {...common}>{paths[name]}</svg>;
}

export default function App(){
  const [data,setData]=useState<Snapshot>(empty),[view,setView]=useState<View>("home"),[activeTerm,setActiveTerm]=useState("autumn-2026"),[invoiceProgramme,setInvoiceProgramme]=useState(""),[mobile,setMobile]=useState(false),[status,setStatus]=useState<"loading"|"saved"|"error">("loading"),[toast,setToast]=useState(""),[resetting,setResetting]=useState(false);
  const [dialog,setDialog]=useState<null|{kind:string;value?:unknown}>(null);
  const [currentUser,setCurrentUser]=useState<CurrentUser>({email:"",displayName:"",role:"Administrator"});
  const flash=(message:string)=>{setToast(message);setTimeout(()=>setToast(""),2800)};
  const apply=(next:Snapshot)=>{setData(next);setStatus("saved");if(!activeTerm&&next.terms[0])setActiveTerm(next.terms[0].id)};
  const mutate=async(action:string,payload?:unknown)=>{setStatus("loading");const r=await fetch("/api/workspace",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,payload})});if(!r.ok){setStatus("error");const issue=await r.json().catch(()=>({error:"The action could not be completed"})) as {error?:string};throw new Error(issue.error||"The action could not be completed")}const next=await r.json() as Snapshot;apply(next);return next};
  useEffect(()=>{fetch("/api/workspace").then(r=>{if(!r.ok)throw new Error();return r.json()}).then((next:Snapshot)=>{setData(next);setStatus("saved")}).catch(()=>setStatus("error"))},[]);
  useEffect(()=>{fetch("/api/me").then(r=>r.json()).then((user:CurrentUser)=>setCurrentUser(user)).catch(()=>undefined)},[]);
  useEffect(()=>{const params=new URLSearchParams(window.location.search);if(params.get("view")==="invoices")setView("invoices");if(params.get("view")==="settings")setView("settings");if(params.get("gmail")==="connected")flash("Gmail connected. Invoice sending is ready.");if(params.get("gmail")==="error")flash(params.get("message")||"Gmail connection failed. Please try again.");if(params.get("email")==="connected")flash(`${params.get("provider")==="microsoft"?"Microsoft":"Google"} connected. Send a safe test before using it for customers.`);if(params.get("email")==="error")flash(params.get("message")||"Email connection failed. Please try again.");if(params.has("gmail")||params.has("email"))window.history.replaceState({},"",window.location.pathname)},[]);
  const calendar=data.settings.find(s=>s.key==="calendar_test_connected")?.value==="true";
  const go=(v:View)=>{setView(v);setMobile(false)};
  const reset=async()=>{if(resetting)return;setResetting(true);try{await mutate("reset");setActiveTerm("autumn-2026");setView("home");flash("Original demo data restored")}catch{flash("Restore failed. Please try again.")}finally{setResetting(false)}};
  return <main className="app-shell">
    <aside className={`sidebar ${mobile?"sidebar-open":""}`}><button className="mobile-close" onClick={()=>setMobile(false)}>×</button><div className="brand"><span className="brand-mark">TG</span><span><strong>Tennis Growth</strong><small>Admin</small></span></div><nav className="main-nav"><p className="nav-label">Workspace</p>{nav.filter(n=>!["invoices","payments","email","reports","settings"].includes(n.id)).map(n=><button key={n.id} className={view===n.id?"active":""} onClick={()=>go(n.id)}><span className="nav-icon"><AppIcon name={n.icon}/></span>{n.label}{n.id==="schedule"&&<span className="nav-count">{data.sessions.length}</span>}</button>)}<p className="nav-label nav-spacer">Finance & contact</p>{nav.filter(n=>["invoices","payments","email","reports"].includes(n.id)).map(n=><button key={n.id} className={view===n.id?"active":""} onClick={()=>go(n.id)}><span className="nav-icon"><AppIcon name={n.icon}/></span>{n.label}{n.id==="invoices"&&<span className="nav-count alert">{data.invoices.filter(i=>i.lifecycleStatus==="Active"&&i.emailStatus!=="Sent").length}</span>}</button>)}</nav><div className="sidebar-bottom"><button className={view==="settings"?"active":""} onClick={()=>go("settings")}><span className="nav-icon"><AppIcon name="settings"/></span>Settings</button><div className="account-card"><span className="avatar">{(currentUser.displayName||currentUser.email||"TG").split(/\s|@/).filter(Boolean).map(part=>part[0]).slice(0,2).join("").toUpperCase()}</span><span><strong>{currentUser.displayName||currentUser.email||"Signed-in user"}</strong><small>{currentUser.role}</small></span></div></div></aside>
    <section className="workspace"><header className="topbar"><button className="menu-button" onClick={()=>setMobile(true)}>☰</button>{view!=="home"&&<button className="back-home" onClick={()=>go("home")}>← Home</button>}<div><p className="eyebrow">Operational Test Version</p><h1>{nav.find(n=>n.id===view)?.label}</h1></div><div className="top-actions"><span className={`data-state ${status}`}>{status==="saved"?"● Database saved":status==="loading"?"○ Saving…":"! Save problem"}</span><button className={`calendar-status ${calendar?"connected":""}`} onClick={()=>go("settings")}><span className="google-g">G</span><span>{calendar?"Test calendar on":"Test Calendar"}</span></button><button className="primary small" onClick={()=>setDialog({kind:"session"})}>+ Add session</button></div></header>
      <div className="test-banner"><span className="test-badge">TEST MODE</span><p><strong>Safe operational testing.</strong> Invoice emails can be sent only to Rob while Test mode is selected. Parent emails are protected and no real payment is taken.</p><div className="test-actions"><button onClick={reset} disabled={resetting}>{resetting?"Restoring…":"Restore demo data"}</button><button className="clear-link" onClick={()=>setDialog({kind:"clearAll"})}>Clear all data</button></div></div>
      <TermBar terms={data.terms} active={activeTerm} setActive={setActiveTerm} add={()=>setDialog({kind:"term"})} edit={()=>{const term=data.terms.find(row=>row.id===activeTerm);if(term)setDialog({kind:"editTerm",value:term})}}/>
      {view==="home"&&<PremiumHome data={data} termId={activeTerm} go={go} open={setDialog}/>}
      {view==="schedule"&&<OperationalSchedule data={data} termId={activeTerm} add={()=>setDialog({kind:"session"})} register={s=>setDialog({kind:"register",value:s})} substitute={s=>setDialog({kind:"substituteCoach",value:s})}/>} 
      {view==="customers"&&<Customers data={data} add={()=>setDialog({kind:"customer"})} edit={c=>setDialog({kind:"customer",value:c})}/>} 
      {view==="programmes"&&<VenueProgrammes data={data} termId={activeTerm} open={setDialog} goInvoices={programmeId=>{setInvoiceProgramme(programmeId);go("invoices")}}/>}
      {view==="camps"&&<VenueProgrammes data={data} termId={activeTerm} open={setDialog} goInvoices={programmeId=>{setInvoiceProgramme(programmeId);go("invoices")}} initialTab="camps"/>}
      {view==="coaches"&&<Coaches data={data} open={setDialog}/>}
      {view==="venues"&&<Venues data={data} go={go}/>}
      {view==="reports"&&<Reports data={data} termId={activeTerm}/>}
      {view==="invoices"&&<ReliableInvoices data={data} termId={activeTerm} initialProgrammeId={invoiceProgramme} mutate={mutate} flash={flash}/>} 
      {view==="payments"&&<EnhancedPayments data={data} record={()=>setDialog({kind:"payment"})}/>}
      {view==="email"&&<GeneralCommunications data={data} mutate={mutate} flash={flash}/>} 
      {view==="settings"&&<><EmailSettingsCard flash={flash}/><UserAccessCard/><SettingsHub data={data} calendar={calendar} mutate={mutate} flash={flash}/></>}
    </section>
    {mobile&&<button className="scrim" onClick={()=>setMobile(false)}/>} 
    {dialog&&<DialogRouter dialog={dialog} close={()=>setDialog(null)} open={setDialog} data={data} termId={activeTerm} mutate={mutate} flash={flash}/>}
    {toast&&<div className="toast"><span>✓</span><div><strong>{toast}</strong><small>Saved in the Operational Test database.</small></div></div>}
  </main>
}

function TermBar({terms,active,setActive,add,edit}:{terms:Term[];active:string;setActive:(v:string)=>void;add:()=>void;edit:()=>void}){return <div className="term-manager"><div><span className="term-label">Working period</span><select aria-label="Select term" value={active} onChange={e=>setActive(e.target.value)}>{terms.map(t=><option key={t.id} value={t.id}>{t.name} · {t.status}</option>)}</select><em>Operational</em></div><div className="term-manager-actions"><button onClick={edit} disabled={!active}>Edit term</button><button onClick={add}>+ Add term</button></div></div>}

function UserAccessCard(){
  const [users,setUsers]=useState<{email:string;displayName:string;role:string;status:string;lastSignedInAt:string}[]>([]);
  useEffect(()=>{fetch("/api/users",{cache:"no-store"}).then(response=>response.ok?response.json():Promise.reject()).then(payload=>setUsers(payload.users||[])).catch(()=>setUsers([]))},[]);
  return <section className="panel user-access-card"><div><span className="section-kicker">Authorised users</span><h3>People who can use this business</h3><p>Each person signs in with their own authorised address. Business records remain shared, while the sending email is connected once for this tennis business.</p></div><div className="new-user-flow"><span><b>1</b><strong>Open the secure link</strong><small>Use the exact authorised sign-in email</small></span><span><b>2</b><strong>Enter Tennis Growth</strong><small>The system confirms the authorised account</small></span><span><b>3</b><strong>Connect business email</strong><small>Settings → Email → Connect Microsoft</small></span><span><b>4</b><strong>Send safe test</strong><small>Then deliberately select Live mode</small></span></div><div className="user-access-list">{users.map(user=><article key={user.email}><span className="user-avatar">{(user.displayName||user.email).split(/\s|@/).filter(Boolean).map(part=>part[0]).slice(0,2).join("").toUpperCase()}</span><span><strong>{user.displayName||user.email}</strong><small>{user.email}</small></span><span><b>{user.role}</b><small>{user.lastSignedInAt?`Last used ${new Date(user.lastSignedInAt).toLocaleDateString("en-GB")}`:"Authorised in the business database"}</small></span><em className="traffic green">{user.status}</em></article>)}</div><footer><div><strong>Jake&apos;s account is authorised</strong><span>Jake must sign in as <b>info@supremetennis.co.uk</b>, then connect that Outlook account from Email Settings.</span></div><form action="/api/auth/signout" method="post"><button className="secondary">Switch account</button></form></footer></section>
}

const paidForInvoice=(data:Snapshot,invoiceId:number)=>data.payments.filter(p=>p.invoiceId===invoiceId).reduce((sum,p)=>sum+p.amountPence,0);
const balanceForInvoice=(data:Snapshot,invoice:Invoice)=>Math.max(0,invoice.amountPence-paidForInvoice(data,invoice.id));

function PremiumHome({data,termId,go,open}:{data:Snapshot;termId:string;go:(v:View)=>void;open:(v:{kind:string;value?:unknown})=>void}){
  const [query,setQuery]=useState(""),[activityFilter,setActivityFilter]=useState("All");
  const today="2026-08-20",hour=new Date().getHours(),greeting=hour<12?"Good morning":hour<18?"Good afternoon":"Good evening";
  const activeInvoices=data.invoices.filter(i=>i.termId===termId&&i.lifecycleStatus==="Active"),ready=activeInvoices.filter(i=>i.emailStatus!=="Sent").length;
  const outstanding=activeInvoices.reduce((sum,i)=>sum+balanceForInvoice(data,i),0),overdue=activeInvoices.filter(i=>balanceForInvoice(data,i)>0&&i.dueDate<today);
  const todaySessions=data.sessions.filter(s=>s.sessionDate===today),missingCoach=data.sessions.filter(s=>!s.coach||s.coach==="Unassigned"),incomplete=todaySessions.filter(s=>!data.attendance.some(a=>a.sessionId===s.id));
  const trials=data.enrolments.filter(e=>e.termId===termId&&e.status==="Trial"),activePlayers=data.customers.filter(c=>c.status==="Active").length;
  const currentTerm=data.terms.find(t=>t.id===termId),nextTerm=data.terms.filter(t=>(currentTerm?.sortOrder??0)<t.sortOrder).sort((a,b)=>a.sortOrder-b.sortOrder)[0];
  const rollover=data.termRollovers.find(r=>r.currentTermId===termId&&r.nextTermId===nextTerm?.id),rolloverRows=data.rolloverDecisions.filter(d=>d.rolloverId===rollover?.id),progressionReviews=rolloverRows.filter(d=>["Recommended","Review Later"].includes(d.progressionStatus)).length;
  const cards:{view:View;title:string;icon:IconName;copy:string;metric:string;tone:string;statusLabel:string}[]=[
    {view:"schedule",title:"Today’s Schedule",icon:"calendar",copy:"Open sessions and complete registers",metric:`${todaySessions.length} session${todaySessions.length===1?"":"s"} today`,tone:incomplete.length?"amber":"blue",statusLabel:incomplete.length?"Needs attention":"Today"},
    {view:"customers",title:"Customers & Players",icon:"users",copy:"Create, update and register players",metric:`${activePlayers} active players`,tone:"green",statusLabel:"Active"},
    {view:"programmes",title:"Programmes",icon:"programmes",copy:"Manage coaching by venue and term",metric:`${data.programmes.length} active programmes`,tone:"purple",statusLabel:"Current"},
    {view:"invoices",title:"Invoices",icon:"invoice",copy:"Create, review and send invoices",metric:`${ready} ready to send`,tone:ready?"amber":"green",statusLabel:ready?"Action needed":"Up to date"},
    {view:"payments",title:"Payments",icon:"payment",copy:"Record payments and track balances",metric:`${money(outstanding)} outstanding`,tone:outstanding?"red":"green",statusLabel:outstanding?"Outstanding":"Paid"},
    {view:"camps",title:"Holiday Camps",icon:"sun",copy:"Manage bookings, capacity and registers",metric:`${data.camps.filter(c=>c.status==="Open").length} active camps`,tone:"amber",statusLabel:"Bookings"},
    {view:"email",title:"Communications",icon:"message",copy:"Email customers and programme groups",metric:`${data.emailOutbox.filter(e=>e.status!=="Sent").length} messages pending`,tone:"blue",statusLabel:"Messages"},
    {view:"coaches",title:"Coaches",icon:"coach",copy:"Manage coaches, assignments and cover",metric:missingCoach.length?`${missingCoach.length} need attention`:`${data.staff.filter(s=>s.status==="Active").length} active coaches`,tone:missingCoach.length?"red":"green",statusLabel:missingCoach.length?"Coach required":"Covered"},
    {view:"venues",title:"Venues",icon:"venue",copy:"Manage locations, courts and sessions",metric:`${data.venues.length} active venues`,tone:"blue",statusLabel:"Locations"},
    {view:"reports",title:"Reports",icon:"report",copy:"View key operational information",metric:"Business overview",tone:"purple",statusLabel:"Insights"}
  ];
  const actionMatches=[
    {terms:"add customer new player",label:"Add Customer",run:()=>open({kind:"customer"})},{terms:"register player enrol",label:"Register Player",run:()=>go("programmes")},{terms:"create invoice send invoice",label:"Create Invoices",run:()=>go("invoices")},{terms:"record payment paid",label:"Record Payment",run:()=>go("payments")},{terms:"email group communicate",label:"Email a Group",run:()=>go("email")},{terms:"schedule attendance register",label:"Open Today’s Schedule",run:()=>go("schedule")},{terms:"coach",label:"Add Coach",run:()=>go("coaches")},{terms:"programme course",label:"Create Programme",run:()=>go("programmes")},{terms:"venue club",label:"Add Venue",run:()=>go("venues")}
  ].filter(a=>`${a.label} ${a.terms}`.toLowerCase().includes(query.toLowerCase()));
  const actions=[
    ...(ready?[{label:`${ready} invoice${ready===1?"":"s"} ready to send`,detail:"Review recipients and PDFs",tone:"amber",view:"invoices" as View}]:[]),
    ...(incomplete.length?[{label:`${incomplete.length} register${incomplete.length===1?"":"s"} incomplete today`,detail:"Complete the attendance record",tone:"amber",view:"schedule" as View}]:[]),
    ...(missingCoach.length?[{label:`${missingCoach.length} session${missingCoach.length===1?"":"s"} have no coach`,detail:"Assign a coach or cover",tone:"red",view:"schedule" as View}]:[]),
    ...(trials.length?[{label:`${trials.length} trial player${trials.length===1?"":"s"} need review`,detail:"Review their programme status",tone:"amber",view:"programmes" as View}]:[]),
    ...(progressionReviews?[{label:`${progressionReviews} progression review${progressionReviews===1?"":"s"} need a decision`,detail:`Review ${nextTerm?.name||"the next term"}`,tone:"amber",view:"programmes" as View}]:[]),
    ...(overdue.length?[{label:`${overdue.length} overdue payment${overdue.length===1?"":"s"}`,detail:"Follow up with the payer",tone:"red",view:"payments" as View}]:[])
  ];
  const activities=[
    ...data.invoiceEmailLogs.map(log=>({category:"Invoices",time:log.sentAt,label:`${data.customers.find(c=>c.id===log.playerId)?.player??log.intendedRecipient} invoice email ${log.sendStatus.toLowerCase()}`})),
    ...data.payments.map(payment=>({category:"Payments",time:payment.paidAt,label:`Payment recorded for ${data.invoices.find(i=>i.id===payment.invoiceId)?.invoiceNumber??payment.reference}`})),
    ...data.customers.map(customer=>({category:"Customers",time:customer.createdAt,label:`${customer.player} added as a customer`}))
  ].filter(a=>activityFilter==="All"||a.category===activityFilter).sort((a,b)=>b.time.localeCompare(a.time)).slice(0,7);
  return <div className="page home-page premium-home"><section className="home-hero"><div><p className="date-line">Thursday, 20 August 2026</p><h2>{greeting}, Rob.</h2><p>Here’s what’s happening with your tennis business today.</p></div><span className="term-chip"><span className="live-dot"/>{data.terms.find(t=>t.id===termId)?.name}</span></section><div className="home-finder action-finder"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="What do you want to do?"/><button disabled={!query||!actionMatches.length} onClick={()=>actionMatches[0]?.run()}>{query&&actionMatches.length?actionMatches[0].label:"Find an action"}</button></div><div className="quick-actions"><button className="quick-primary" onClick={()=>open({kind:"customer"})}>+ Add Customer</button><button onClick={()=>go("programmes")}>Register Player</button><button onClick={()=>go("invoices")}>Create Invoices</button><button onClick={()=>go("schedule")}>Open Schedule</button></div><section className="home-card-grid premium-card-grid">{cards.map(card=><button key={card.title} className={`home-card premium-card status-${card.tone}`} onClick={()=>go(card.view)}><span className={`home-card-icon ${card.tone}`}><AppIcon name={card.icon}/></span><span><span className={`card-status-label ${card.tone}`}><i/>{card.statusLabel}</span><strong>{card.title}</strong><small>{card.copy}</small><em className={`traffic ${card.tone}`}>{card.metric}</em></span><b>›</b></button>)}</section>{nextTerm&&<section className="panel next-term-card"><div><span className="section-kicker">Next Term</span><h3>{nextTerm.name}</h3><p>{rollover?`${rolloverRows.filter(r=>r.continuationStatus==="Continuing").length} continuing · ${rolloverRows.filter(r=>r.continuationStatus==="Not Continuing").length} not continuing · ${rolloverRows.filter(r=>r.continuationStatus==="Awaiting Confirmation").length} awaiting confirmation`:`Prepare continuation and progression decisions without creating or sending invoices.`}</p></div><div><em className={`traffic ${rollover?.status==="Complete"?"green":progressionReviews?"amber":"blue"}`}>{rollover?.status||"Not prepared"}</em><button className="primary" onClick={()=>open({kind:"termRollover",value:{currentTermId:termId,nextTermId:nextTerm.id}})}>{rollover?"Review Next Term":"Prepare Next Term"}</button></div></section>}<section className="home-lower-grid"><article className="panel action-panel"><div className="panel-head"><div><span className="section-kicker">Today</span><h3>Action Required</h3></div></div>{actions.length?actions.map(a=><button key={a.label} onClick={()=>a.label.includes("progression")&&nextTerm?open({kind:"termRollover",value:{currentTermId:termId,nextTermId:nextTerm.id}}):go(a.view)}><span className={`traffic-dot ${a.tone}`}/><span><strong>{a.label}</strong><small>{a.detail}</small></span><b>›</b></button>):<div className="all-clear"><span>✓</span><div><strong>All clear</strong><small>There are no urgent actions at the moment.</small></div></div>}</article><article className="panel activity-panel"><div className="panel-head"><div><span className="section-kicker">Saved activity</span><h3>Recent Activity</h3></div></div><div className="activity-filters">{["All","Invoices","Payments","Customers"].map(filter=><button className={activityFilter===filter?"active":""} key={filter} onClick={()=>setActivityFilter(filter)}>{filter}</button>)}</div>{activities.length?<div className="activity-list">{activities.map((activity,index)=><div key={`${activity.time}-${index}`}><time>{activity.time.includes("T")?new Date(activity.time).toLocaleTimeString("en-GB",{hour:"2-digit",minute:"2-digit"}):activity.time}</time><span className={`activity-dot ${activity.category.toLowerCase()}`}/><p><strong>{activity.label}</strong><small>{activity.category}</small></p></div>)}</div>:<div className="activity-empty">No saved activity in this filter yet.</div>}</article></section></div>;
}

function HomeCommandCentre({data,termId,go,open}:{data:Snapshot;termId:string;go:(v:View)=>void;open:(v:{kind:string;value?:unknown})=>void}){
  const [query,setQuery]=useState("");
  const today="2026-08-20",todaySessions=data.sessions.filter(s=>s.sessionDate===today),activeCustomers=data.customers.filter(c=>c.status==="Active").length;
  const unsentInvoices=data.invoices.filter(i=>i.termId===termId&&i.lifecycleStatus==="Active"&&i.emailStatus!=="Sent");
  const ready=unsentInvoices.length,overdue=data.invoices.filter(i=>i.termId===termId&&i.lifecycleStatus==="Active"&&i.paymentStatus!=="Paid"&&i.dueDate<today),outstanding=data.invoices.filter(i=>i.termId===termId&&i.lifecycleStatus==="Active"&&i.paymentStatus!=="Paid").reduce((n,i)=>n+i.amountPence,0);
  const trials=data.enrolments.filter(e=>e.termId===termId&&e.status==="Trial").length,missingCoach=data.sessions.filter(s=>!s.coach||s.coach==="Unassigned").length;
  const incomplete=todaySessions.filter(s=>!data.attendance.some(a=>a.sessionId===s.id)).length;
  const cards:{view:View;title:string;icon:string;copy:string;status:string;tone:string}[]=[
    {view:"schedule",title:"Today’s Schedule",icon:"□",copy:"Open today’s sessions and registers",status:`${todaySessions.length} sessions today`,tone:"blue"},
    {view:"customers",title:"Customers & Players",icon:"○",copy:"Create, update and register players",status:`${activeCustomers} active players`,tone:"green"},
    {view:"programmes",title:"Programmes",icon:"▤",copy:"Work by venue, programme and term",status:`${data.programmes.length} active programmes`,tone:"blue"},
    {view:"programmes",title:"Holiday Camps",icon:"☀",copy:"Bookings, capacity and camp registers",status:`${data.camps.filter(c=>c.status==="Open").length} camps open`,tone:"amber"},
    {view:"invoices",title:"Invoices",icon:"£",copy:"Create, review and send invoices",status:`${ready} ready to send`,tone:ready?"amber":"green"},
    {view:"payments",title:"Payments",icon:"✓",copy:"Record payments and track balances",status:`${money(outstanding)} outstanding`,tone:outstanding?"red":"green"},
    {view:"email",title:"Communications",icon:"↗",copy:"Send updates to selected groups",status:`${data.emailOutbox.length} messages recorded`,tone:"blue"},
    {view:"coaches",title:"Coaches",icon:"♟",copy:"Records, assignments and cover",status:missingCoach?`${missingCoach} sessions need a coach`:`${data.staff.filter(s=>s.status==="Active").length} active coaches`,tone:missingCoach?"red":"green"},
    {view:"venues",title:"Venues",icon:"⌖",copy:"See activity at each location",status:`${data.venues.length} active venues`,tone:"blue"},
    {view:"reports",title:"Reports",icon:"▥",copy:"Operational totals without clutter",status:"Current term summary",tone:"blue"},
    {view:"settings",title:"Settings",icon:"⚙",copy:"Business, email and calendar setup",status:"Manage business setup",tone:"blue"}
  ];
  const actions=[
    {label:`${ready} invoice${ready===1?"":"s"} ready to send`,view:"invoices" as View,tone:ready?"amber":"green",detail:ready?"Review recipients and PDFs":"Nothing waiting"},
    {label:`${trials} trial player${trials===1?"":"s"} need reviewing`,view:"programmes" as View,tone:trials?"amber":"green",detail:trials?"Change trial status when ready":"No trials waiting"},
    {label:missingCoach?`${missingCoach} session${missingCoach===1?"":"s"} have no coach`:"Every session has a coach",view:"schedule" as View,tone:missingCoach?"red":"green",detail:missingCoach?"Assign cover before the session":"Coach cover complete"},
    {label:`${overdue.length} overdue payment${overdue.length===1?"":"s"}`,view:"payments" as View,tone:overdue.length?"red":"green",detail:overdue.length?"Follow up with the payer":"No overdue payments"},
    {label:`${incomplete} register${incomplete===1?"":"s"} incomplete today`,view:"schedule" as View,tone:incomplete?"amber":"green",detail:incomplete?"Complete after each session":"Today’s registers complete"}
  ];
  const matching=cards.filter(card=>`${card.title} ${card.copy}`.toLowerCase().includes(query.toLowerCase()));
  const setup=[{label:"Business details",done:Boolean(data.settings.find(s=>s.key==="business_name")?.value),action:()=>go("settings")},{label:"Venue",done:data.venues.length>0,action:()=>go("venues")},{label:"Coach",done:data.staff.length>0,action:()=>go("coaches")},{label:"Connect Gmail",done:Boolean(data.settings.find(s=>s.key==="gmail_refresh_token")?.value),action:()=>go("invoices")},{label:"Programme",done:data.programmes.length>0,action:()=>go("programmes")},{label:"Customer",done:data.customers.length>0,action:()=>open({kind:"customer"})}],completeCount=setup.filter(s=>s.done).length;
  return <div className="page home-page"><div className="home-welcome"><div><p className="date-line">Thursday, 20 August 2026</p><h2>What would you like to do?</h2><p>Search for a task, choose a main area or deal with something needing attention.</p></div><span className="term-chip"><span className="live-dot"/>{data.terms.find(t=>t.id===termId)?.name}</span></div><div className="home-finder"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Try ‘invoice’, ‘add player’, ‘coach’ or ‘schedule’"/><button disabled={!matching.length} onClick={()=>matching[0]&&go(matching[0].view)}>{matching.length?`Open ${matching[0].title}`:"No matching area"}</button></div><div className="quick-actions"><button onClick={()=>open({kind:"customer"})}>+ Add customer</button><button onClick={()=>go("programmes")}>Register player</button><button onClick={()=>go("invoices")}>Create invoices</button><button onClick={()=>go("schedule")}>View today</button></div>{completeCount<setup.length&&<section className="panel setup-checklist"><div><span className="section-kicker">Getting started</span><h3>{completeCount} of {setup.length} setup tasks complete</h3></div>{setup.map(item=><button key={item.label} onClick={item.action}><span className={item.done?"done":"todo"}>{item.done?"✓":"○"}</span>{item.label}<b>›</b></button>)}</section>}<section className="home-card-grid">{matching.map((card,index)=><button key={`${card.title}-${index}`} className="home-card" onClick={()=>go(card.view)}><span className={`home-card-icon ${card.tone}`}>{card.icon}</span><span><strong>{card.title}</strong><small>{card.copy}</small><em className={`traffic ${card.tone}`}>{card.status}</em></span><b>›</b></button>)}</section><section className="panel action-panel"><div className="panel-head"><div><span className="section-kicker">Today</span><h3>Action required</h3></div><span className="traffic blue">Only current tasks are shown</span></div>{actions.map(a=><button key={a.label} onClick={()=>go(a.view)}><span className={`traffic-dot ${a.tone}`}/><span><strong>{a.label}</strong><small>{a.detail}</small></span><b>›</b></button>)}</section></div>
}

function Dashboard({data,termId,go,openRegister}:{data:Snapshot;termId:string;go:(v:View)=>void;openRegister:(s:Session)=>void}){const sessions=data.sessions.filter(s=>s.termId===termId),outstanding=data.invoices.filter(i=>i.status!=="Paid").reduce((n,i)=>n+i.amountPence,0),revenue=data.payments.reduce((n,p)=>n+p.amountPence,0),capacity=data.programmes.reduce((n,p)=>n+p.capacity,0),currentEnrolments=data.enrolments.filter(e=>e.termId===termId&&e.status!=="Inactive"),activeEnrolments=currentEnrolments.filter(e=>e.status==="Active");return <div className="page"><section className="welcome-row"><div><p className="date-line">Thursday, 20 August 2026</p><h2>Good morning, Rob.</h2><p>Your operational test workspace is ready for realistic trial journeys.</p></div><div className="term-chip"><span className="live-dot"/>{data.terms.find(t=>t.id===termId)?.name??"Choose a term"}</div></section><section className="metric-grid"><Metric icon="↗" tone="blue" label="Active players" value={String(data.customers.filter(c=>c.status==="Active").length)} note={`${activeEnrolments.length} active enrolments`}/><Metric icon="£" tone="yellow" label="Test payments" value={money(revenue)} note={`${data.payments.length} recorded`}/><Metric icon="!" tone="red" label="Outstanding" value={money(outstanding)} note={`${data.invoices.filter(i=>i.status!=="Paid").length} invoices`}/><Metric icon="✓" tone="green" label="Places used" value={`${currentEnrolments.length} / ${capacity}`} note="Database calculated"/></section><section className="dashboard-grid"><article className="panel schedule-panel"><div className="panel-head"><div><span className="section-kicker">Current term</span><h3>Coaching schedule</h3></div><button className="text-button" onClick={()=>go("schedule")}>Full schedule →</button></div><div className="timeline">{sessions.slice(0,5).map(s=><div className="timeline-row" key={s.id}><time>{s.start}</time><span className={`timeline-line ${s.tone}`}/><div className="session-copy"><strong>{s.title}</strong><span>{s.venue} · {s.coach}</span></div><span className="player-count">{s.meta}</span><button className="ghost-button" onClick={()=>openRegister(s)}>Register</button></div>)}</div></article><aside className="right-stack"><article className="panel calendar-card"><div className="calendar-title"><span className="google-g large">G</span><div><span className="section-kicker">Calendar test</span><h3>{data.settings.find(s=>s.key==="calendar_test_connected")?.value==="true"?"Test sync active":"Test diary not connected"}</h3></div></div><p>Validate conflicts and coaching diary behaviour without sharing a real Google account.</p><button className="primary full" onClick={()=>go("settings")}>Manage calendar test</button></article><article className="panel attention-card"><div className="panel-head"><div><span className="section-kicker">Operational status</span><h3>Five connected phases</h3></div></div>{[["○",`${data.customers.length} customer records`,"Customers and enrolments"],["£",`${data.invoices.length} test invoices`,"PDF records available"],["↗",`${data.emailOutbox.length} outbox records`,"No real delivery"]].map(a=><button key={a[1]} onClick={()=>go(a[0]==="£"?"invoices":a[0]==="↗"?"email":"customers")}><span className="attention-icon blue">{a[0]}</span><span><strong>{a[1]}</strong><small>{a[2]}</small></span><b>›</b></button>)}</article></aside></section></div>}
function Metric({icon,tone,label,value,note}:{icon:string;tone:string;label:string;value:string;note:string}){return <article className="metric-card"><div className={`metric-icon ${tone}`}>{icon}</div><div><span>{label}</span><strong>{value}</strong><small className={tone==="red"?"negative":"positive"}>{note}</small></div></article>}

function Schedule({data,termId,add,register}:{data:Snapshot;termId:string;add:()=>void;register:(s:Session)=>void}){const [mode,setMode]=useState("Week"),[venue,setVenue]=useState("All"),[coach,setCoach]=useState("All"),[week,setWeek]=useState(0);const rows=data.sessions.filter(s=>s.termId===termId&&(venue==="All"||s.venue===venue)&&(coach==="All"||s.coach===coach));return <div className="page"><div className="page-intro"><div><h2>Schedule</h2><p>Filter, move through weeks and open a database-linked register.</p></div><button className="primary" onClick={add}>+ Add session</button></div><div className="page-toolbar"><div className="segmented">{["Week","Day","Staff"].map(m=><button className={mode===m?"selected":""} onClick={()=>setMode(m)} key={m}>{m}</button>)}</div><div className="filter-row"><select value={venue} onChange={e=>setVenue(e.target.value)}><option>All</option>{data.venues.map(v=><option key={v.id}>{v.name}</option>)}</select><select value={coach} onChange={e=>setCoach(e.target.value)}><option>All</option>{data.staff.map(s=><option key={s.id}>{s.name}</option>)}</select></div><div className="week-control"><button onClick={()=>setWeek(week-1)}>‹</button><strong>{mode} · week {week+1}</strong><button onClick={()=>setWeek(week+1)}>›</button></div></div><section className="panel ops-list"><div className="ops-head"><span>Day</span><span>Time</span><span>Session</span><span>Venue and coach</span><span>Action</span></div>{rows.map(s=><div className="ops-row" key={s.id}><span>{["Mon","Tue","Wed","Thu","Fri","Sat","Sun"][s.day]}</span><strong>{s.start}</strong><span><strong>{s.title}</strong><small>{s.duration} mins · {s.meta}</small></span><span><strong>{s.venue}</strong><small>{s.coach}</small></span><button className="secondary" onClick={()=>register(s)}>Register</button></div>)}</section></div>}

function Customers({data,add,edit}:{data:Snapshot;add:()=>void;edit:(c:Customer)=>void}){
  const [q,setQ]=useState(""),[programme,setProgramme]=useState("All"),[recordStatus,setRecordStatus]=useState("Current");
  const rows=data.customers.filter(c=>(recordStatus==="All"||(recordStatus==="Archived"?c.status==="Inactive":c.status!=="Inactive"))&&(programme==="All"||c.programme===programme)&&Object.values(c).join(" ").toLowerCase().includes(q.toLowerCase()));
  const exportCsv=()=>{const csv=["Player,Payer,Email,Phone,Programme,Balance",...rows.map(c=>[c.player,c.payer,c.email,c.phone,c.programme,c.balance].map(x=>`"${x}"`).join(","))].join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([csv],{type:"text/csv"}));a.download="supreme-tennis-test-customers.csv";a.click();URL.revokeObjectURL(a.href)};
  return <div className="page"><div className="page-intro"><div><h2>Customers and players</h2><p>Archive former players without losing their account history.</p></div><button className="primary" onClick={add}>+ Add customer</button></div><div className="table-tools"><label className="search-box"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search player, parent or programme"/></label><select aria-label="Filter customer status" value={recordStatus} onChange={e=>setRecordStatus(e.target.value)}><option>Current</option><option>Archived</option><option>All</option></select><select value={programme} onChange={e=>setProgramme(e.target.value)}><option>All</option>{data.programmes.map(p=><option key={p.id}>{p.name}</option>)}</select><button className="secondary" onClick={exportCsv}>Export CSV</button></div><section className="data-table panel"><div className="table-row table-head"><span>Player</span><span>Parent or payer</span><span>Programme</span><span>Balance</span><span>Status</span><span/></div>{rows.map(c=><button className="table-row customer-row" key={c.id} onClick={()=>edit(c)}><span><b className="mini-avatar">{c.player.split(" ").map(x=>x[0]).join("")}</b><span><strong>{c.player}</strong><small>{c.id} · {c.consent} consent</small></span></span><span><strong>{c.payer}</strong><small>{c.email}</small></span><span>{c.programme}</span><span><strong>{c.balance}</strong></span><span><em className={`status-pill ${c.status==="Inactive"?"archived":c.payment.toLowerCase().replace(" ","-")}`}>{c.status==="Inactive"?"Archived":c.payment}</em></span><b>›</b></button>)}</section></div>
}

function Programmes({data,termId,open,goInvoices}:{data:Snapshot;termId:string;open:(v:{kind:string;value?:unknown})=>void;goInvoices:(programmeId:string)=>void}){const [tab,setTab]=useState<"terms"|"camps">("terms");return <div className="page"><div className="page-intro"><div><h2>Programmes and holiday camps</h2><p>Database-backed groups, enrolments, camps and bookings.</p></div><button className="primary" onClick={()=>open({kind:tab==="terms"?"programme":"camp"})}>+ New {tab==="terms"?"programme":"camp"}</button></div><div className="programme-tabs"><button className={tab==="terms"?"active":""} onClick={()=>setTab("terms")}>Term programmes</button><button className={tab==="camps"?"active":""} onClick={()=>setTab("camps")}>Holiday camps <span>{data.camps.length}</span></button></div>{tab==="terms"?<div className="programme-grid">{data.programmes.map(p=>{const registrations=data.enrolments.filter(e=>e.programmeId===p.id&&e.termId===termId&&e.status!=="Inactive"),active=registrations.filter(e=>e.status==="Active").length,trials=registrations.filter(e=>e.status==="Trial").length;return <article className="programme-card panel" key={p.id}><span className={`programme-tone ${p.tone}`}/><div className="programme-title"><div><h3>{p.name}</h3><p>{["Mon","Tue","Wed","Thu","Fri","Sat","Sun"][p.weekday]} · {p.startTime}</p></div><span className="status-pill paid">{p.status}</span></div><div className="programme-meta"><span><small>Venue</small><strong>{p.venue}</strong></span><span><small>Coach</small><strong>{p.coach}</strong></span></div><div className="capacity-row"><span><strong>{registrations.length}</strong> of {p.capacity} places · {active} active{trials?` · ${trials} trial${trials===1?"":"s"}`:""}</span><b>{Math.round(registrations.length/p.capacity*100)}%</b></div><div className="progress"><i style={{width:`${Math.min(100,registrations.length/p.capacity*100)}%`}}/></div><div className="card-actions programme-actions"><button className="secondary" onClick={()=>open({kind:"players",value:p})}>View register</button><button className="secondary" onClick={()=>open({kind:"registerPlayer",value:p})}>+ Add player</button><button className="primary" onClick={()=>goInvoices(p.id)}>Create invoices</button></div></article>})}</div>:<div className="camp-list">{data.camps.map(c=>{const booked=data.campBookings.filter(b=>b.campName===c.name).length;return <article className="panel camp-card" key={c.id}><div className="camp-date"><span>{new Date(c.startDate+"T12:00:00Z").getUTCDate()}</span><small>{new Date(c.startDate+"T12:00:00Z").toLocaleDateString("en-GB",{month:"short",year:"numeric"})}</small></div><div className="camp-main"><div><span className={`status-pill ${c.status==="Open"?"paid":"draft"}`}>{c.status}</span><h3>{c.name}</h3><p>{c.venue} · {c.time}</p></div><div className="camp-numbers"><span><small>Bookings</small><strong>{booked} / {c.capacity}</strong></span><span><small>Price</small><strong>{money(c.dayPricePence)} day / {money(c.fullPricePence)} full</strong></span></div><div className="progress"><i style={{width:`${booked/c.capacity*100}%`}}/></div></div><div className="camp-actions"><button className="secondary" onClick={()=>open({kind:"campRegister",value:c})}>View register</button><button className="primary" onClick={()=>open({kind:"campBooking",value:c})}>+ Test booking</button></div></article>})}</div>}</div>}

function Invoices({data,termId,initialProgrammeId,mutate,flash,goEmail}:{data:Snapshot;termId:string;initialProgrammeId:string;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void;goEmail:()=>void}){
  const firstEnrolled=data.programmes.find(p=>data.enrolments.some(e=>e.programmeId===p.id&&e.termId===termId))?.id;
  const [programmeId,setProgrammeId]=useState(initialProgrammeId||firstEnrolled||data.programmes[0]?.id||"mini-red");
  const [selected,setSelected]=useState<string[]>([]),[generating,setGenerating]=useState(false),[generated,setGenerated]=useState<number[]>([]),[emailing,setEmailing]=useState<number|null>(null);
  const [gmail,setGmail]=useState<GmailConnection|null>(null),[sendingBatch,setSendingBatch]=useState(false),[showGmailSetup,setShowGmailSetup]=useState(false),[sendReview,setSendReview]=useState<Invoice[]|null>(null);
  const eligible=data.enrolments.filter(e=>e.programmeId===programmeId&&e.termId===termId&&e.status==="Active").map(e=>data.customers.find(c=>c.id===e.customerId)).filter(c=>c&&c.status!=="Inactive") as Customer[];
  const alreadyInvoiced=new Set(data.invoices.filter(i=>i.programmeId===programmeId&&i.termId===termId).map(i=>i.customerId));
  const candidates=eligible.filter(c=>!alreadyInvoiced.has(c.id));
  const programme=data.programmes.find(p=>p.id===programmeId),term=data.terms.find(t=>t.id===termId),each=(programme?.pricePence??0)*(term?.sessionCount??0);
  useEffect(()=>setSelected(candidates.map(c=>c.id)),[programmeId,termId,data.enrolments.length,data.invoices.length]);
  useEffect(()=>{fetch("/api/email/status").then(r=>r.json()).then((status:GmailConnection)=>setGmail({...status,configured:true})).catch(()=>setGmail({configured:false,connected:false,email:"robnorris125@gmail.com",testRecipient:"robnorris125@gmail.com",deliveryMode:"test",error:"Email status unavailable"}))},[]);
  const batchInvoices=data.invoices.filter(i=>i.programmeId===programmeId&&i.termId===termId&&i.status!=="Sent");

  const generate=async()=>{
    if(!selected.length||generating)return;
    setGenerating(true);
    const before=new Set(data.invoices.map(i=>i.id));
    try{
      const next=await mutate("generateInvoices",{customerIds:selected,programmeId,termId});
      const created=next.invoices.filter(i=>!before.has(i.id));
      setGenerated(created.map(i=>i.id));
      flash(`${created.length} invoice${created.length===1?"":"s"} created and email${created.length===1?"":"s"} prepared`);
      setTimeout(()=>document.getElementById("invoice-review")?.scrollIntoView({behavior:"smooth",block:"start"}),80);
    }catch(error){flash(error instanceof Error?error.message:"Invoice creation failed. Please try again.")}finally{setGenerating(false)}
  };

  const emailParent=async(invoice:Invoice,confirmed=false)=>{
    if(emailing!==null||sendingBatch)return;
    setEmailing(invoice.id);
    try{
      if(!gmail?.configured){setShowGmailSetup(true);return}
      if(!gmail.connected){window.location.assign("/api/gmail/connect");return}
      const destination=gmail.deliveryMode==="test"?gmail.testRecipient:data.customers.find(c=>c.id===invoice.customerId)?.email;
      if(!confirmed){setSendReview([invoice]);return}
      const next=await mutate("sendInvoice",{invoiceId:invoice.id});
      const sent=next.invoices.find(i=>i.id===invoice.id)?.status==="Sent";
      flash(sent?`Invoice email and PDF sent to ${destination}`:"Gmail could not send this invoice. Check the email record and retry.");
    }catch(error){
      flash(error instanceof Error?error.message:"The invoice email and PDF could not be sent.");
    }finally{setEmailing(null)}
  };

  const sendAll=async(confirmed=false,invoiceIds=batchInvoices.map(i=>i.id))=>{
    const selectedBatch=batchInvoices.filter(i=>invoiceIds.includes(i.id));
    if(sendingBatch||!selectedBatch.length)return;
    if(!gmail?.configured){setShowGmailSetup(true);return}
    if(!gmail.connected){window.location.assign("/api/gmail/connect");return}
    const destination=gmail.deliveryMode==="test"?gmail.testRecipient:"the stored parent email addresses";
    if(!confirmed){setSendReview(selectedBatch);return}
    setSendingBatch(true);
    try{
      const next=await mutate("sendInvoiceBatch",{invoiceIds:selectedBatch.map(i=>i.id)});
      const sent=selectedBatch.filter(i=>next.invoices.find(n=>n.id===i.id)?.status==="Sent").length;
      flash(`${sent} of ${selectedBatch.length} invoice email${selectedBatch.length===1?"":"s"} sent with PDF attachments`);
    }catch(error){flash(error instanceof Error?error.message:"The Gmail batch could not be sent. No parent addresses were used in test mode.")}finally{setSendingBatch(false)}
  };

  const changeDeliveryMode=async(mode:"test"|"live")=>{
    if(!gmail||gmail.deliveryMode===mode)return;
    if(mode==="live"&&!window.confirm("Switch to live delivery? Future invoice emails will go to the parent or client address saved in each record."))return;
    try{await mutate("setSetting",{key:"email_delivery_mode",value:mode});setGmail({...gmail,deliveryMode:mode});flash(mode==="test"?"Test delivery restored. Emails will only go to Rob.":"Live delivery selected. Emails will use the stored parent addresses.")}catch{flash("Email delivery mode could not be changed.")}
  };

  return <div className="page">
    <div className="page-intro"><div><p className="section-kicker">Phase 2 · Database invoicing</p><h2>Group invoices</h2><p>Create a separate invoice and professional parent email draft for every selected player.</p></div><select aria-label="Invoice programme" value={programmeId} onChange={e=>{setProgrammeId(e.target.value);setGenerated([])}}>{data.programmes.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></div>
    <div className="invoice-info"><span>i</span><p><strong>{gmail?.deliveryMode==="live"?"Live parent delivery.":"Safe Gmail test mode."}</strong> {gmail?.deliveryMode==="live"?"Each email and attached PDF will go to the parent or client address saved in their record.":<>Every email and attached PDF is redirected to <strong>robnorris125@gmail.com</strong>.</>}</p></div>
    {generated.length>0&&<div className="action-success"><span>✓</span><div><strong>{generated.length} invoice{generated.length===1?"":"s"} created successfully</strong><small>The PDF is ready and each personalised email is in the test outbox.</small></div><button className="secondary" onClick={goEmail}>Review parent emails</button></div>}
    <section className="invoice-table panel"><div className="invoice-row invoice-head"><label><input type="checkbox" checked={selected.length===candidates.length&&candidates.length>0} disabled={!candidates.length} onChange={()=>setSelected(selected.length===candidates.length?[]:candidates.map(c=>c.id))}/><span>Player and payer</span></label><span>Sessions</span><span>Status</span><span>Amount</span></div>{eligible.length===0?<div className="invoice-empty"><strong>No enrolled players in this programme</strong><span>Choose another programme or add the player to this group first.</span></div>:eligible.map(c=>{const invoiced=alreadyInvoiced.has(c.id);return <div className="invoice-row" key={c.id}><label>{invoiced?<span className="invoice-done" aria-label="Already invoiced">✓</span>:<input type="checkbox" checked={selected.includes(c.id)} onChange={()=>setSelected(selected.includes(c.id)?selected.filter(x=>x!==c.id):[...selected,c.id])}/>}<span><strong>{c.player}</strong><small>{c.payer} · {c.email}</small></span></label><span>{term?.sessionCount}</span><span>{invoiced?<em className="status-pill paid">Already invoiced</em>:"Ready"}</span><span><strong>{money(each)}</strong></span></div>})}</section>
    <div className="sticky-action"><div><strong>{selected.length?`${selected.length} invoice${selected.length===1?"":"s"} selected`:candidates.length?"No players selected":"Invoice already created"}</strong><span>{selected.length?`Total ${money(each*selected.length)}`:candidates.length?"Select at least one player":"Continue to the saved record to email it with its PDF"}</span></div>{candidates.length?<button className="primary" disabled={!selected.length||generating} onClick={generate}>{generating?"Creating invoices…":selected.length?`Create ${selected.length} invoice${selected.length===1?"":"s"} + emails`:"Select a player"}</button>:<a className="primary link-button" href="#saved-invoices">Go to saved invoice</a>}</div>
    <section className="panel gmail-batch-panel"><div className="gmail-batch-copy"><span className={`gmail-connection-dot ${gmail?.connected?"connected":""}`}/><div><p className="section-kicker">Email delivery</p><h3>{gmail?.connected?"Rob’s Gmail is ready":gmail?.configured?"One approval left":"Connect the sending account once"}</h3><p>{gmail?.connected?`${batchInvoices.length} unsent invoice${batchInvoices.length===1?"":"s"} in this group. The system matches each invoice to the email saved against that parent or client.`:gmail?.configured?"Approve Rob’s Gmail once. The system will remember the sender and attach the correct PDF automatically.":"Upload the Google setup file you already downloaded. The system fills the technical details for you."}</p><div className="email-routing-summary"><span><small>From</small><strong>Rob’s Gmail</strong></span><b>→</b><span><small>To</small><strong>{gmail?.deliveryMode==="live"?"Each saved parent email":"Rob only while testing"}</strong></span><b>+</b><span><small>Attached</small><strong>Correct invoice PDF</strong></span></div><div className="delivery-mode"><button className={gmail?.deliveryMode==="test"?"active":""} onClick={()=>void changeDeliveryMode("test")}>Test to Rob</button><button className={gmail?.deliveryMode==="live"?"active live":""} onClick={()=>void changeDeliveryMode("live")}>Live to parents</button></div></div></div><div className="gmail-batch-actions"><button className="primary" disabled={Boolean(gmail?.connected&&(!batchInvoices.length||sendingBatch))} onClick={()=>{if(!gmail?.configured){setShowGmailSetup(true);return}if(!gmail.connected){window.location.assign("/api/gmail/connect");return}void sendAll()}}>{sendingBatch?"Sending separately…":!gmail?.configured?"Connect sending Gmail":!gmail.connected?"Approve Gmail connection":batchInvoices.length?`Send all ${batchInvoices.length} email${batchInvoices.length===1?"":"s"} + PDFs`:"All invoices sent"}</button></div></section>
    <div className="saved-invoice-heading" id="invoice-review"><div><h3>Review and send invoices</h3><p>Check each payer, recipient, amount and PDF before sending.</p></div><span className="traffic amber">Prepared invoices are not sent yet</span></div>
    <section className="panel ops-list invoice-records">{data.invoices.length===0?<div className="empty-focus"><span>£</span><h3>No invoices generated yet</h3><p>Select a group above and generate its operational test invoices.</p></div>:data.invoices.slice().reverse().map(i=>{const c=data.customers.find(x=>x.id===i.customerId);return <div className={`ops-row invoice-record ${generated.includes(i.id)?"new-record":""}`} key={i.id}><span><strong>{i.invoiceNumber}</strong><small>{c?.player} · {c?.payer}</small></span><span><small>Status</small><strong>{i.status}</strong></span><span><small>Due</small><strong>{new Date(`${i.dueDate}T12:00:00Z`).toLocaleDateString("en-GB")}</strong></span><span><small>Total</small><strong>{money(i.amountPence)}</strong></span><div className="invoice-actions"><a className="secondary link-button" href={`/api/invoice?invoiceId=${i.id}&download=1`} target="_blank" rel="noreferrer" download={`${i.invoiceNumber}.pdf`}>Preview PDF</a><button className="primary" disabled={emailing!==null||sendingBatch||i.status==="Sent"} onClick={()=>void emailParent(i)}>{i.status==="Sent"?"Email + PDF sent":emailing===i.id?"Sending…":!gmail?.configured?"Set up Gmail":!gmail.connected?"Connect Gmail":"Send email + PDF"}</button></div></div>})}</section>
    {showGmailSetup&&<GmailSetup close={()=>setShowGmailSetup(false)} flash={flash}/>} 
    {sendReview&&<InvoiceSendReview invoices={sendReview} data={data} gmail={gmail} close={()=>setSendReview(null)} send={ids=>{const rows=sendReview.filter(i=>ids.includes(i.id));setSendReview(null);if(rows.length===1)void emailParent(rows[0],true);else void sendAll(true,ids)}}/>}
  </div>
}

function ReliableInvoices({data,termId,initialProgrammeId,mutate,flash}:{data:Snapshot;termId:string;initialProgrammeId:string;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){
  const firstEnrolled=data.programmes.find(p=>data.enrolments.some(e=>e.programmeId===p.id&&e.termId===termId))?.id;
  const [programmeId,setProgrammeId]=useState(initialProgrammeId||firstEnrolled||data.programmes[0]?.id||"");
  const [selected,setSelected]=useState<string[]>([]),[generatedIds,setGeneratedIds]=useState<number[]>([]),[reviewIds,setReviewIds]=useState<number[]>([]);
  const [sessionCounts,setSessionCounts]=useState<Record<string,number>>({});
  const [gmail,setGmail]=useState<GmailConnection|null>(null),[working,setWorking]=useState(false),[showSetup,setShowSetup]=useState(false),[confirmRows,setConfirmRows]=useState<Invoice[]|null>(null),[results,setResults]=useState<InvoiceEmailLog[]|null>(null),[resendId,setResendId]=useState<number|null>(null),[duplicateChoice,setDuplicateChoice]=useState<Invoice|null>(null),[replacementTarget,setReplacementTarget]=useState<Invoice|null>(null);
  const programme=data.programmes.find(p=>p.id===programmeId),term=data.terms.find(t=>t.id===termId);
  const registrations=data.enrolments.filter(e=>e.programmeId===programmeId&&e.termId===termId);
  const activeInvoices=data.invoices.filter(i=>i.lifecycleStatus==="Active");
  const rows=registrations.map(registration=>{const matching=activeInvoices.filter(i=>i.customerId===registration.customerId&&i.programmeId===programmeId&&i.termId===termId);return {registration,customer:data.customers.find(c=>c.id===registration.customerId),invoice:matching[0],invoices:matching}}).filter(row=>row.customer) as {registration:Enrolment;customer:Customer;invoice:Invoice|undefined;invoices:Invoice[]}[];
  const readyPlayers=rows.filter(row=>row.registration.status==="Active"&&row.customer.status==="Active"&&!row.invoices.length);
  const already=rows.filter(row=>row.invoices.length>0);
  const excluded=rows.filter(row=>row.registration.status!=="Active"||row.customer.status!=="Active");
  const keyFor=(i:Invoice)=>`${i.customerId}|${i.programmeId}|${i.termId}`;
  const duplicateGroups=Object.values(activeInvoices.filter(i=>i.programmeId===programmeId&&i.termId===termId).reduce<Record<string,Invoice[]>>((groups,invoice)=>{const key=keyFor(invoice);(groups[key]??=[]).push(invoice);return groups},{})).filter(group=>group.length>1);
  const duplicateKeys=new Set(duplicateGroups.flatMap(group=>group.map(keyFor)));
  const hasDuplicates=duplicateGroups.length>0;
  const readyInvoices=activeInvoices.filter(i=>i.programmeId===programmeId&&i.termId===termId&&i.emailStatus!=="Sent"&&!duplicateKeys.has(keyFor(i)));
  const allReady=activeInvoices.filter(i=>i.termId===termId&&i.emailStatus!=="Sent");
  const sentToday=data.invoiceEmailLogs.filter(log=>log.sendStatus==="Sent"&&log.sentAt.slice(0,10)==="2026-08-20");
  const overdue=activeInvoices.filter(i=>i.termId===termId&&i.paymentStatus!=="Paid"&&i.dueDate<"2026-08-20");
  useEffect(()=>{setSelected(readyPlayers.map(row=>row.customer.id));setSessionCounts(Object.fromEntries(readyPlayers.map(row=>[row.customer.id,term?.sessionCount??1])))},[programmeId,termId,data.invoices.length,data.enrolments.length,term?.sessionCount]);
  useEffect(()=>setReviewIds(data.invoices.filter(i=>i.programmeId===programmeId&&i.termId===termId&&i.lifecycleStatus==="Active"&&i.emailStatus!=="Sent"&&!duplicateKeys.has(keyFor(i))).map(i=>i.id)),[programmeId,termId,data.invoices.length,hasDuplicates]);
  useEffect(()=>{fetch("/api/email/status").then(r=>r.json()).then((value:GmailConnection)=>setGmail({...value,configured:true})).catch(()=>setGmail({configured:false,connected:false,email:"robnorris125@gmail.com",testRecipient:"robnorris125@gmail.com",deliveryMode:"test"}))},[]);

  const create=async()=>{
    if(!selected.length||working)return;setWorking(true);
    const before=new Set(data.invoices.map(i=>i.id));
    try{
      const next=await mutate("generateInvoices",{customerIds:selected,programmeId,termId,sessionCounts});
      const created=next.invoices.filter(i=>!before.has(i.id));
      setGeneratedIds(created.map(i=>i.id));setReviewIds(created.map(i=>i.id));
      flash(`${created.length} invoice${created.length===1?"":"s"} created. PDFs and personalised emails are ready to review.`);
      setTimeout(()=>document.getElementById("review-send")?.scrollIntoView({behavior:"smooth",block:"start"}),80);
    }catch(error){flash(error instanceof Error?error.message:"Invoices could not be created")}finally{setWorking(false)}
  };
  const setMode=async(mode:"test"|"live")=>{if(!gmail||gmail.deliveryMode===mode)return;try{await mutate("setSetting",{key:"email_delivery_mode",value:mode});setGmail({...gmail,deliveryMode:mode});flash(mode==="test"?"Test mode active. Every email goes only to Rob.":"Live mode selected. The confirmation screen will show every customer address.")}catch{flash("Delivery mode could not be changed")}};
  const openSend=(invoices:Invoice[],isResend=false)=>{if(hasDuplicates){flash("Duplicate invoice detected. Choose the valid invoice and void the duplicate before sending.");return}if(!gmail?.configured){setShowSetup(true);return}if(!gmail.connected){window.location.assign("/api/gmail/connect");return}setResendId(isResend&&invoices[0]?invoices[0].id:null);setConfirmRows(invoices)};
  const send=async(ids:number[])=>{
    if(working)return;setWorking(true);setConfirmRows(null);
    try{
      const next=resendId&&ids.length===1?await mutate("resendInvoice",{invoiceId:ids[0]}):await mutate("sendInvoiceBatch",{invoiceIds:ids});
      const latest=ids.map(id=>next.invoiceEmailLogs.filter(log=>log.invoiceId===id).at(-1)).filter(Boolean) as InvoiceEmailLog[];
      setResults(latest);setReviewIds(ids.filter(id=>latest.find(log=>log.invoiceId===id)?.sendStatus!=="Sent"));
      const sent=latest.filter(log=>log.sendStatus==="Sent").length,failed=latest.filter(log=>log.sendStatus==="Failed").length;
      flash(failed?`${sent} sent, ${failed} failed. Review the result below.`:`${sent} invoice email${sent===1?"":"s"} sent with matching PDFs.`);
      setTimeout(()=>document.getElementById("send-results")?.scrollIntoView({behavior:"smooth",block:"start"}),80);
    }catch(error){flash(error instanceof Error?error.message:"Invoice emails could not be sent")}finally{setWorking(false);setResendId(null)}
  };
  const retryFailed=()=>{const ids=(results??[]).filter(log=>log.sendStatus==="Failed").map(log=>log.invoiceId),invoices=data.invoices.filter(i=>ids.includes(i.id));if(invoices.length)openSend(invoices)};
  const keepInvoice=async(invoice:Invoice)=>{if(working)return;setWorking(true);try{await mutate("resolveInvoiceDuplicates",{keepInvoiceId:invoice.id});flash(`${invoice.invoiceNumber} kept. The other duplicate invoice is now voided and cannot be sent or paid.`);setDuplicateChoice(null)}catch(error){flash(error instanceof Error?error.message:"The duplicate could not be resolved")}finally{setWorking(false)}};
  const createReplacement=async(invoice:Invoice)=>{if(working)return;setWorking(true);try{const before=new Set(data.invoices.map(i=>i.id)),next=await mutate("createReplacementInvoice",{invoiceId:invoice.id}),replacement=next.invoices.find(i=>!before.has(i.id));if(replacement){setReviewIds([replacement.id]);setGeneratedIds([replacement.id])}flash(`${invoice.invoiceNumber} replaced. The original is no longer payable or sendable.`);setReplacementTarget(null)}catch(error){flash(error instanceof Error?error.message:"The replacement invoice could not be created")}finally{setWorking(false)}};
  const latestLog=(invoiceId:number)=>data.invoiceEmailLogs.filter(log=>log.invoiceId===invoiceId).at(-1);
  const reviewInvoices=(reviewIds.length?data.invoices.filter(i=>reviewIds.includes(i.id)):readyInvoices).filter(i=>i.emailStatus!=="Sent");
  const visibleReviewInvoices=readyInvoices,selectedReviewInvoices=visibleReviewInvoices.filter(i=>reviewIds.includes(i.id));
  const selectedTotal=selected.reduce((total,customerId)=>total+(programme?.pricePence??0)*(sessionCounts[customerId]??term?.sessionCount??0),0);
  const groups=data.venues.map(venue=>({venue,programmes:data.programmes.filter(p=>p.venue===venue.name).map(p=>({programme:p,invoices:data.invoices.filter(i=>i.programmeId===p.id&&i.termId===termId)})).filter(group=>group.invoices.length)})).filter(group=>group.programmes.length);
  const activity=[...data.invoiceEmailLogs.map(log=>({time:log.sentAt,copy:`${data.invoices.find(i=>i.id===log.invoiceId)?.invoiceNumber} email ${log.sendStatus.toLowerCase()}`,tone:log.sendStatus==="Sent"?"green":"red"})),...data.invoices.map(i=>({time:i.createdAt,copy:`${i.invoiceNumber} PDF and email prepared`,tone:"amber"}))].sort((a,b)=>b.time.localeCompare(a.time)).slice(0,6);

  return <div className="page reliable-invoices">
    <div className="page-intro"><div><p className="section-kicker">One guided workflow</p><h2>Invoice a programme</h2><p>Choose the group, confirm who needs an invoice, create the records, then send each email with its matching PDF.</p></div><select aria-label="Invoice programme" value={programmeId} onChange={e=>{setProgrammeId(e.target.value);setGeneratedIds([]);setReviewIds([]);setResults(null)}}>{data.programmes.map(p=><option value={p.id} key={p.id}>{p.venue} · {p.name} · {p.startTime}</option>)}</select></div>
    <div className="invoice-steps" aria-label="Invoice workflow"><span className="active">1 Review players</span><b>›</b><span className={generatedIds.length?"active":""}>2 Create invoices</span><b>›</b><span className={reviewInvoices.length?"active":""}>3 Review and send</span><b>›</b><span className={results?"active":""}>4 Confirmation</span></div>
    <section className="panel eligibility-card"><div className="eligibility-title"><div><span className="section-kicker">{programme?.venue}</span><h3>{programme?.name}</h3><p>{["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"][programme?.weekday??0]} {programme?.startTime} · {term?.name}</p></div><div className="eligibility-stats"><span><strong>{rows.filter(r=>r.registration.status==="Active"&&r.customer.status==="Active").length}</strong><small>active players</small></span><span><strong>{already.length}</strong><small>already invoiced</small></span><span className="needs"><strong>{readyPlayers.length}</strong><small>need invoices</small></span><span><strong>{excluded.length}</strong><small>excluded</small></span></div></div>
      {hasDuplicates&&<div className="duplicate-warning" role="alert"><span>!</span><div><strong>Duplicate invoice detected</strong><p>Group sending is disabled. Choose which invoice is valid; the other record will be retained as Voided and cannot be sent or paid.</p></div></div>}
      {duplicateGroups.map(group=>{const customer=data.customers.find(c=>c.id===group[0].customerId);return <section className="duplicate-resolution" key={keyFor(group[0])}><header><div><strong>{customer?.player}</strong><span>{programme?.name} · {term?.name}</span></div><em>Choose the valid invoice</em></header>{group.map(invoice=>{const log=latestLog(invoice.id);return <div key={invoice.id}><span><strong>{invoice.invoiceNumber}</strong><small>Created {new Date(invoice.createdAt).toLocaleString("en-GB")}</small></span><span><strong>{money(invoice.amountPence)}</strong><small>Email: {invoice.emailStatus} · Payment: {invoice.paymentStatus}</small></span><span><strong>{log?`Last send: ${log.sendStatus}`:"Never sent"}</strong><small>{log?new Date(log.sentAt).toLocaleString("en-GB"):"No send history"}</small></span><button className="primary compact" onClick={()=>setDuplicateChoice(invoice)}>Keep this invoice</button></div>})}</section>})}
      <div className="eligibility-list">{rows.length?rows.map(({registration,customer,invoice,invoices})=>{const duplicate=invoices.length>1,selectable=registration.status==="Active"&&customer.status==="Active"&&!invoices.length,count=sessionCounts[customer.id]??term?.sessionCount??1;return <div className={`eligibility-row ${duplicate?"duplicate-row":""}`} key={customer.id}><label>{selectable?<input type="checkbox" checked={selected.includes(customer.id)} onChange={()=>setSelected(selected.includes(customer.id)?selected.filter(id=>id!==customer.id):[...selected,customer.id])}/>:<span className={`row-state ${invoice?"done":"excluded"}`}>{duplicate?"!":invoice?"✓":"–"}</span>}<span><strong>{customer.player}</strong><small>{customer.payer} · {customer.email}</small></span></label><span><strong>{duplicate?`${invoices.length} active invoices`:invoice?invoice.invoiceNumber:selectable?"Ready to invoice":registration.status}</strong><small>{duplicate?"Duplicate invoice detected · sending blocked":invoice?"Already invoiced · excluded":selectable?`${count} of ${term?.sessionCount} sessions · ${money((programme?.pricePence??0)*count)}`:"Not eligible for group invoicing"}</small></span>{selectable?<label className="session-count-control"><span>Sessions to bill</span><select value={count} onChange={event=>setSessionCounts({...sessionCounts,[customer.id]:Number(event.target.value)})}>{Array.from({length:term?.sessionCount??1},(_,index)=><option key={index+1} value={index+1}>{index+1} session{index===0?"":"s"}</option>)}</select></label>:invoice&&!duplicate?<button className="secondary compact" onClick={()=>openSend([invoice],true)}>Resend invoice</button>:<span className={`email-state ${duplicate?"failed":"draft"}`}>{duplicate?"Resolve duplicate":"Excluded"}</span>}</div>}) : <div className="invoice-empty"><strong>No players registered in this programme</strong><span>Register players first, then return to Invoice Group.</span></div>}</div>
      <div className="pro-rata-note"><strong>Part-term starters</strong><span>Choose the exact number of remaining sessions for each player. Their individual amount and PDF will be calculated automatically.</span></div>
      <div className="eligibility-action"><div><strong>{hasDuplicates?"Resolve the duplicate first":`${selected.length} selected`}</strong><span>{hasDuplicates?"No further group invoices can be created or sent until one valid record is chosen.":selected.length?`${money(selectedTotal)} total · separate invoice for each player`:"Already-invoiced and ineligible players are protected"}</span></div><button className="primary" disabled={hasDuplicates||!selected.length||working} title={hasDuplicates?"Resolve the duplicate invoice before creating more group invoices":undefined} onClick={()=>void create()}>{hasDuplicates?"Resolve duplicate before creating":working?"Creating and checking PDFs…":`Create ${selected.length} Invoice${selected.length===1?"":"s"}`}</button></div>
    </section>

    <section className="panel review-send-card" id="review-send"><div className="panel-head"><div><span className="section-kicker">Review and send</span><h3>{reviewInvoices.length?`Ready to send ${reviewInvoices.length} invoice${reviewInvoices.length===1?"":"s"}`:"Nothing waiting to send"}</h3></div><div className="gmail-compact"><span className={gmail?.connected?"connected":""}>{gmail?.connected?"✓":"!"}</span><strong>{gmail?.connected?"Gmail connected":"Gmail needs connecting"}</strong><button onClick={()=>setShowSetup(true)}>Manage connection</button></div></div>
      <div className={`mode-banner ${gmail?.deliveryMode||"test"}`}><div><strong>{gmail?.deliveryMode==="live"?"LIVE MODE":"TEST MODE"}</strong><span>From: {gmail?.email||"robnorris125@gmail.com"}</span><span>{gmail?.deliveryMode==="live"?"To: each customer’s saved email":`Actual recipient: ${gmail?.testRecipient||"robnorris125@gmail.com"}`}</span></div><div className="delivery-mode"><button className={gmail?.deliveryMode==="test"?"active":""} onClick={()=>void setMode("test")}>Test to Rob</button><button className={gmail?.deliveryMode==="live"?"active live":""} onClick={()=>void setMode("live")}>Live to parents</button></div></div>
      <div className="review-table">{visibleReviewInvoices.map(invoice=>{const customer=data.customers.find(c=>c.id===invoice.customerId),programmeRow=data.programmes.find(p=>p.id===invoice.programmeId),item=data.invoiceItems.find(row=>row.invoiceId===invoice.id),checked=reviewIds.includes(invoice.id);return <label className="review-row" key={invoice.id}><input type="checkbox" checked={checked} onChange={()=>setReviewIds(checked?reviewIds.filter(id=>id!==invoice.id):[...reviewIds,invoice.id])}/><span><strong>{customer?.player}</strong><small>Parent: {customer?.payer}</small><small>Intended recipient: {customer?.email}</small></span><span><strong>{invoice.invoiceNumber}</strong><small>{programmeRow?.name} · {term?.name}</small><small>{item?.quantity??term?.sessionCount} session{(item?.quantity??term?.sessionCount)===1?"":"s"} billed</small></span><span><strong>{money(invoice.amountPence)}</strong><small className="pdf-ready">✓ PDF ready</small><small className={`email-state ${invoice.emailStatus.toLowerCase().replaceAll(" ","-")}`}>{invoice.emailStatus}</small></span></label>})}</div>
      <div className="eligibility-action"><div><strong>{hasDuplicates?"Sending disabled":`${selectedReviewInvoices.length} email${selectedReviewInvoices.length===1?"":"s"} selected`}</strong><span>{hasDuplicates?"Resolve the duplicate invoice shown above before any group email can be sent.":"Every recipient receives a separate private email and matching PDF."}</span></div><button className="primary" disabled={hasDuplicates||!selectedReviewInvoices.length||working} title={hasDuplicates?"Duplicate active invoices must be resolved before sending":undefined} onClick={()=>openSend(selectedReviewInvoices)}>{hasDuplicates?"Resolve duplicate before sending":gmail?.deliveryMode==="live"?`Send ${selectedReviewInvoices.length} Live Emails & PDFs`:`Send ${selectedReviewInvoices.length} Test Emails & PDFs`}</button></div>
    </section>

    {results&&<section className="panel send-results" id="send-results"><div className="result-heading"><span className={results.every(r=>r.sendStatus==="Sent")?"success":"partial"}>{results.every(r=>r.sendStatus==="Sent")?"✓":"!"}</span><div><p className="section-kicker">Send complete</p><h3>{results.filter(r=>r.sendStatus==="Sent").length} sent, {results.filter(r=>r.sendStatus==="Failed").length} failed</h3><p>The database has been updated from Gmail’s response.</p></div></div>{results.map(log=>{const invoice=data.invoices.find(i=>i.id===log.invoiceId),customer=data.customers.find(c=>c.id===invoice?.customerId);return <div className="result-row" key={log.id}><span><strong>{customer?.player}</strong><small>{invoice?.invoiceNumber} · {log.actualRecipient}</small></span><span><em className={`email-state ${log.sendStatus.toLowerCase()}`}>{log.sendStatus}</em><small>{new Date(log.sentAt).toLocaleTimeString("en-GB",{hour:"2-digit",minute:"2-digit"})} · {log.pdfAttached?"PDF attached":"No PDF"}</small>{log.failureReason&&<small className="failure-reason">{log.failureReason}</small>}</span></div>})}{results.some(r=>r.sendStatus==="Failed")&&<button className="primary" onClick={retryFailed}>Retry failed email{results.filter(r=>r.sendStatus==="Failed").length===1?"":"s"}</button>}</section>}

    <section className="invoice-overview-grid"><article className="panel next-actions"><span className="section-kicker">Next actions</span><h3>What still needs doing</h3><button><strong>{allReady.length}</strong><span>invoices ready to send</span></button><button><strong>{activeInvoices.filter(i=>i.termId===termId&&i.paymentStatus==="Outstanding").length}</strong><span>awaiting payment</span></button><button><strong>{overdue.length}</strong><span>overdue invoices</span></button><button><strong>{sentToday.length}</strong><span>sent today</span></button></article><article className="panel recent-invoice-activity"><span className="section-kicker">Recent invoice activity</span><h3>Today and latest changes</h3>{activity.length?activity.map((item,index)=><div key={`${item.time}-${index}`}><time>{new Date(item.time).toLocaleTimeString("en-GB",{hour:"2-digit",minute:"2-digit"})}</time><span className={`traffic-dot ${item.tone}`}/><strong>{item.copy}</strong></div>):<p>No invoice activity yet.</p>}</article></section>

    <div className="saved-invoice-heading"><div><h3>Invoice records</h3><p>Grouped by venue, programme and term. Voided and replaced records remain visible for a safe audit trail.</p></div></div><div className="invoice-groups">{groups.map(group=><section className="panel" key={group.venue.id}><h3>{group.venue.name}</h3>{group.programmes.map(programmeGroup=>{const active=programmeGroup.invoices.filter(i=>i.lifecycleStatus==="Active"),sent=active.filter(i=>i.emailStatus==="Sent").length,paid=active.filter(i=>i.paymentStatus==="Paid").length,outstanding=active.filter(i=>i.paymentStatus!=="Paid").reduce((n,i)=>n+i.amountPence,0);return <details key={programmeGroup.programme.id}><summary><span><strong>{programmeGroup.programme.name} · {programmeGroup.programme.startTime}</strong><small>{term?.name}</small></span><span><strong>{active.length} active invoice{active.length===1?"":"s"}</strong><small>{sent} sent · {paid} paid · {money(outstanding)} outstanding</small></span><b>View invoices</b></summary><div className="group-invoices">{programmeGroup.invoices.map(invoice=>{const customer=data.customers.find(c=>c.id===invoice.customerId),log=latestLog(invoice.id),inactive=invoice.lifecycleStatus!=="Active";return <div className={inactive?"inactive-invoice":""} key={invoice.id}><span><strong>{invoice.invoiceNumber}</strong><small>{customer?.player} · {customer?.payer}</small><small>Created {new Date(invoice.createdAt).toLocaleString("en-GB")}</small></span><span><em className={`email-state ${inactive?"draft":invoice.emailStatus.toLowerCase().replaceAll(" ","-")}`}>{inactive?invoice.lifecycleStatus:invoice.emailStatus}</em><small>{log?.sentAt?`${log.actualRecipient} · ${new Date(log.sentAt).toLocaleString("en-GB")}`:"Not sent"}</small></span><span><strong>{money(invoice.amountPence)}</strong><small>Payment: {inactive?"Not payable":invoice.paymentStatus}</small></span><div className="record-invoice-actions"><a className="secondary link-button" href={`/api/invoice?invoiceId=${invoice.id}`} target="_blank" rel="noreferrer">Open PDF</a>{!inactive&&<button className="secondary" onClick={()=>setReplacementTarget(invoice)}>Create replacement</button>}</div></div>})}</div></details>})}</section>)}</div>

    <section className="panel communication-history"><div className="panel-head"><div><span className="section-kicker">Communication history</span><h3>Invoice email audit trail</h3></div><span className="traffic blue">Saved permanently</span></div>{data.invoiceEmailLogs.length?data.invoiceEmailLogs.slice().reverse().map(log=>{const invoice=data.invoices.find(i=>i.id===log.invoiceId),customer=data.customers.find(c=>c.id===invoice?.customerId);return <div className="history-row" key={log.id}><span><strong>{invoice?.invoiceNumber} · {customer?.player}</strong><small>{customer?.payer} · intended: {log.intendedRecipient}</small></span><span><strong>{log.actualRecipient}</strong><small>From {log.sendingAccount} · {log.deliveryMode.toUpperCase()}</small></span><span><em className={`email-state ${log.sendStatus.toLowerCase()}`}>{log.sendStatus}</em><small>{new Date(log.sentAt).toLocaleString("en-GB")} · {log.pdfAttached?"PDF attached":"No PDF"}</small>{log.failureReason&&<small className="failure-reason">{log.failureReason}</small>}</span></div>}):<p>No invoice emails have been attempted yet.</p>}</section>
    {showSetup&&<GmailSetup close={()=>setShowSetup(false)} flash={flash}/>} {confirmRows&&<InvoiceSendReview invoices={confirmRows} data={data} gmail={gmail} close={()=>setConfirmRows(null)} send={ids=>void send(ids)}/>} 
    {duplicateChoice&&<Modal title={`Keep ${duplicateChoice.invoiceNumber}?`} kicker="Resolve duplicate safely" close={()=>setDuplicateChoice(null)}><p className="modal-copy">This invoice will remain the valid financial record. Every other active invoice for the same player, programme and term will be marked Voided, retained in the audit trail, and removed from sending and payment totals.</p><div className="check-list"><span><strong>Player:</strong> {data.customers.find(c=>c.id===duplicateChoice.customerId)?.player}</span><span><strong>Programme:</strong> {data.programmes.find(p=>p.id===duplicateChoice.programmeId)?.name}</span><span><strong>Term:</strong> {data.terms.find(t=>t.id===duplicateChoice.termId)?.name}</span><span><strong>Amount:</strong> {money(duplicateChoice.amountPence)}</span><span><strong>Email status:</strong> {duplicateChoice.emailStatus} · <strong>Payment:</strong> {duplicateChoice.paymentStatus}</span></div><div className="modal-actions"><button className="secondary" onClick={()=>setDuplicateChoice(null)}>Cancel</button><button className="danger-button" disabled={working} onClick={()=>void keepInvoice(duplicateChoice)}>{working?"Resolving…":"Keep this and void duplicates"}</button></div></Modal>}
    {replacementTarget&&<Modal title={`Replace ${replacementTarget.invoiceNumber}?`} kicker="Deliberate replacement invoice" close={()=>setReplacementTarget(null)}><p className="modal-copy">A new invoice number and matching PDF will be created. The original will become Replaced and will no longer be payable or sendable. Use Resend invoice instead when the existing invoice is still correct.</p><div className="check-list"><span><strong>Player:</strong> {data.customers.find(c=>c.id===replacementTarget.customerId)?.player}</span><span><strong>Programme:</strong> {data.programmes.find(p=>p.id===replacementTarget.programmeId)?.name}</span><span><strong>Term:</strong> {data.terms.find(t=>t.id===replacementTarget.termId)?.name}</span><span><strong>Amount:</strong> {money(replacementTarget.amountPence)}</span></div><div className="modal-actions"><button className="secondary" onClick={()=>setReplacementTarget(null)}>Cancel</button><button className="danger-button" disabled={working} onClick={()=>void createReplacement(replacementTarget)}>{working?"Creating…":"Create replacement invoice"}</button></div></Modal>}
  </div>
}

function GmailSetup({close,flash}:{close:()=>void;flash:(m:string)=>void}){
  const [clientId,setClientId]=useState(""),[clientSecret,setClientSecret]=useState(""),[fileName,setFileName]=useState(""),[fileError,setFileError]=useState(""),[saving,setSaving]=useState(false);
  const redirect=`${process.env.NEXT_PUBLIC_APP_URL||"https://admin.tennisgrowth.com"}/api/gmail/callback`;
  const copy=async()=>{try{await navigator.clipboard.writeText(redirect);flash("Google redirect address copied")}catch{flash("Copy failed. Press and hold the address to copy it.")}};
  const loadGoogleFile=async(file:File|undefined)=>{
    if(!file)return;
    setFileError("");setFileName("");
    try{
      const json=JSON.parse(await file.text()) as {web?:{client_id?:string;client_secret?:string;redirect_uris?:string[]};installed?:{client_id?:string;client_secret?:string;redirect_uris?:string[]}};
      const credentials=json.web??json.installed;
      if(!credentials?.client_id||!credentials.client_secret)throw new Error("This is not a Google OAuth credentials file");
      if(!credentials.client_id.endsWith(".apps.googleusercontent.com"))throw new Error("The file does not contain a valid Google Client ID");
      if(credentials.redirect_uris&&!credentials.redirect_uris.includes(redirect))throw new Error("The Google file does not contain the Tennis Growth Admin redirect address");
      setClientId(credentials.client_id);setClientSecret(credentials.client_secret);setFileName(file.name);
    }catch(error){setClientId("");setClientSecret("");setFileError(error instanceof Error?error.message:"The Google file could not be read")}
  };
  const save=async()=>{if(saving)return;setSaving(true);try{const response=await fetch("/api/gmail/setup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({clientId,clientSecret})});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error);window.location.assign("/api/gmail/connect")}catch(error){flash(error instanceof Error?error.message:"Gmail setup could not be saved");setSaving(false)}};
  return <div className="modal-wrap" role="presentation"><button className="modal-scrim" onClick={close} aria-label="Close Gmail setup"/><section className="confirm-modal wide-modal gmail-setup-modal" role="dialog" aria-modal="true" aria-labelledby="gmail-setup-title"><div className="modal-title"><div><p className="section-kicker">One-time email connection</p><h2 id="gmail-setup-title">Finish connecting Rob&apos;s Gmail</h2></div><button onClick={close} aria-label="Close">×</button></div><p className="gmail-setup-lead">You have already created and downloaded Google&apos;s setup file. Upload it here and Tennis Growth Admin will safely fill the technical details.</p><div className="simple-gmail-flow"><div><b>1</b><span><strong>Upload the Google file</strong><small>Select the JSON file saved in Downloads.</small></span></div><div><b>2</b><span><strong>Approve Rob&apos;s Gmail</strong><small>Google asks permission to send invoice emails only.</small></span></div><div><b>3</b><span><strong>Send one safe test</strong><small>The first email and PDF go only to Rob.</small></span></div></div><label className={`google-file-upload ${fileName?"ready":""}`}><input type="file" accept="application/json,.json" onChange={e=>void loadGoogleFile(e.target.files?.[0])}/><span>{fileName?"✓":"↑"}</span><div><strong>{fileName?"Google file ready":"Choose Google setup file"}</strong><small>{fileName?fileName:"Look in your Downloads folder for a .json file"}</small></div></label>{fileError&&<p className="file-error">{fileError}</p>}{fileName&&<div className="connection-preview"><span><small>Sending from</small><strong>robnorris125@gmail.com</strong></span><span><small>Recipients</small><strong>Rob in test mode, then saved parent emails</strong></span><span><small>Each email includes</small><strong>The matching invoice PDF</strong></span></div>}<details className="gmail-advanced"><summary>Enter the Google details manually instead</summary><label className="setup-field">Google Client ID<input autoCapitalize="none" autoCorrect="off" value={clientId} onChange={e=>setClientId(e.target.value)} placeholder="…apps.googleusercontent.com"/></label><label className="setup-field">Google Client secret<input type="password" autoCapitalize="none" autoCorrect="off" value={clientSecret} onChange={e=>setClientSecret(e.target.value)} placeholder="Paste the secret from Google"/></label><button className="redirect-copy" onClick={()=>void copy()}>{redirect}<b>Copy redirect</b></button></details><p className="secure-note">The file is not stored. The two connection values are protected and your Google password is never requested.</p><div className="modal-actions"><button className="secondary" onClick={close}>Cancel</button><button className="primary" disabled={saving||!clientId.trim()||!clientSecret.trim()} onClick={()=>void save()}>{saving?"Connecting safely…":"Continue to Google approval"}</button></div></section></div>
}

function EnhancedPayments({data,record}:{data:Snapshot;record:()=>void}){const active=data.invoices.filter(i=>i.lifecycleStatus==="Active"),outstanding=active.reduce((sum,i)=>sum+balanceForInvoice(data,i),0),overdue=active.filter(i=>balanceForInvoice(data,i)>0&&i.dueDate<"2026-08-20").reduce((sum,i)=>sum+balanceForInvoice(data,i),0),paidThisMonth=data.payments.filter(p=>p.paidAt.startsWith("2026-08")).reduce((sum,p)=>sum+p.amountPence,0),awaiting=active.filter(i=>balanceForInvoice(data,i)>0).length,termId=data.terms.find(t=>t.status==="Current")?.id??"autumn-2026",paidThisTerm=data.payments.filter(p=>data.invoices.find(i=>i.id===p.invoiceId)?.termId===termId).reduce((sum,p)=>sum+p.amountPence,0);return <div className="page payments-page"><div className="page-intro"><div><p className="section-kicker">Financial position</p><h2>Payments</h2><p>Record payments, match them to invoices and see the remaining balance immediately.</p></div><button className="primary" disabled={!awaiting} onClick={record}>{awaiting?"+ Record Payment":"No payments outstanding"}</button></div><section className="payment-overview"><Metric icon="£" tone="red" label="Total outstanding" value={money(outstanding)} note={`${awaiting} invoice${awaiting===1?"":"s"} awaiting payment`}/><Metric icon="!" tone="yellow" label="Overdue" value={money(overdue)} note={overdue?"Follow-up required":"Nothing overdue"}/><Metric icon="✓" tone="green" label="Paid this term" value={money(paidThisTerm)} note="From saved payment records"/><Metric icon="↗" tone="blue" label="Paid this month" value={money(paidThisMonth)} note="August 2026"/></section><section className="panel payment-table enhanced-payment-table"><div className="payment-row payment-head"><span>Date</span><span>Player</span><span>Invoice</span><span>Method</span><span>Amount</span><span>Status</span></div>{data.payments.length?data.payments.slice().reverse().map(p=>{const invoice=data.invoices.find(i=>i.id===p.invoiceId),balance=invoice?balanceForInvoice(data,invoice):0;return <div className="payment-row" key={p.id}><span>{p.paidAt}</span><span><strong>{data.customers.find(c=>c.id===p.customerId)?.player}</strong><small>{p.notes||"No note"}</small></span><span><strong>{invoice?.invoiceNumber??p.reference}</strong></span><span>{p.method}</span><span><strong>{money(p.amountPence)}</strong></span><span><em className={`status-pill ${balance?"part-paid":"paid"}`}>{balance?`Part Paid · ${money(balance)} left`:"Paid"}</em></span></div>}):<div className="payment-empty"><strong>No payments recorded yet</strong><span>Use Record Payment when an invoice is paid.</span></div>}</section></div>}

function GeneralCommunications({data,mutate,flash}:{data:Snapshot;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){
  const [programme,setProgramme]=useState("All"),[subject,setSubject]=useState("Supreme Tennis session update"),[body,setBody]=useState("Hi {{parent_first_name}},\n\nA quick update about {{player_first_name}} and {{programme_name}}.");
  const recipients=data.customers.filter(c=>c.status!=="Inactive"&&(programme==="All"||c.programme===programme));
  const generalRows=data.emailOutbox.filter(row=>!data.invoices.some(invoice=>row.subject.includes(invoice.invoiceNumber)));
  const prepare=async()=>{try{await mutate("queueEmails",{customerIds:recipients.map(c=>c.id),subject,body});flash(`${recipients.length} separate general messages prepared`)}catch(error){flash(error instanceof Error?error.message:"Messages could not be prepared")}};
  return <div className="page"><div className="page-intro"><div><p className="section-kicker">General communications</p><h2>Contact customers</h2><p>Programme updates, cancellations and general messages only. Invoice emails are created and sent inside Invoices.</p></div></div><div className="invoice-info"><span>i</span><p><strong>Invoice delivery stays separate.</strong> To send an invoice and PDF, open Invoices or use Invoice Group from a programme.</p></div><section className="compose-grid"><article className="panel compose-card"><label>Recipients<select value={programme} onChange={e=>setProgramme(e.target.value)}><option>All</option>{data.programmes.map(p=><option key={p.id}>{p.name}</option>)}</select></label><label>Subject<input value={subject} onChange={e=>setSubject(e.target.value)}/></label><label>Message<textarea value={body} onChange={e=>setBody(e.target.value)}/></label><div className="compose-actions"><span className="safe-label">GENERAL MESSAGE</span><button className="primary" disabled={!recipients.length} onClick={()=>void prepare()}>Prepare {recipients.length} separate message{recipients.length===1?"":"s"}</button></div></article><aside className="panel smart-fields"><h3>Personalisation preview</h3><p>Each recipient gets a separate message.</p><div className="email-preview"><small>Example</small><p>{body.replaceAll("{{parent_first_name}}",recipients[0]?.payer.split(" ")[0]??"Parent").replaceAll("{{player_first_name}}",recipients[0]?.player.split(" ")[0]??"Player").replaceAll("{{programme_name}}",recipients[0]?.programme??"Programme")}</p></div></aside></section><h3 className="subheading">General message history</h3><section className="email-draft-list">{generalRows.length?generalRows.slice().reverse().map(row=>{const customer=data.customers.find(c=>c.id===row.customerId);return <article className="panel email-draft-card" key={row.id}><header><div><span className="section-kicker">{row.status}</span><h3>{customer?.payer} · {customer?.player}</h3><p><strong>To:</strong> {row.recipient}</p><p><strong>Subject:</strong> {row.subject}</p></div><time>{new Date(row.createdAt).toLocaleString("en-GB")}</time></header><details><summary>Review personalised message</summary><pre>{row.body}</pre></details></article>}):<div className="panel invoice-empty"><strong>No general messages prepared</strong><span>Invoice emails are recorded in Invoices, not here.</span></div>}</section></div>
}

function EmailCentre({data,mutate,flash}:{data:Snapshot;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){
  const [programme,setProgramme]=useState("All"),[subject,setSubject]=useState("Supreme Tennis session update"),[body,setBody]=useState("Hi {{parent_first_name}},\n\nA quick update about {{player_first_name}} and {{programme_name}}."),[refreshing,setRefreshing]=useState<number|null>(null),[sending,setSending]=useState<number|null>(null),[gmail,setGmail]=useState<GmailConnection|null>(null);
  const recipients=data.customers.filter(c=>c.status!=="Inactive"&&(programme==="All"||c.programme===programme));
  useEffect(()=>{fetch("/api/email/status").then(r=>r.json()).then((status:GmailConnection)=>setGmail({...status,configured:true})).catch(()=>setGmail({configured:false,connected:false,email:"robnorris125@gmail.com",testRecipient:"robnorris125@gmail.com",deliveryMode:"test",error:"Email status unavailable"}))},[]);
  const prepare=async()=>{await mutate("queueEmails",{customerIds:recipients.map(c=>c.id),subject,body});flash(`${recipients.length} personalised emails added to the test outbox`)};
  const copy=async(value:string)=>{try{await navigator.clipboard.writeText(value);flash("Email message copied")}catch{flash("Copy failed. Select the message and copy it manually.")}};
  const refresh=async(invoiceId:number)=>{setRefreshing(invoiceId);try{await mutate("refreshInvoiceEmail",{invoiceId});flash("Invoice email draft refreshed with current details")}catch(error){flash(error instanceof Error?error.message:"Email draft could not be refreshed")}finally{setRefreshing(null)}};
  const send=async(invoice:Invoice,recipient:string)=>{
    if(sending!==null)return;
    if(!gmail?.configured){flash("Finish the Gmail setup from Invoices first.");return}
    if(!gmail.connected){window.location.assign("/api/gmail/connect");return}
    const destination=gmail.deliveryMode==="test"?gmail.testRecipient:recipient;
    if(!window.confirm(`Send ${invoice.invoiceNumber} with its PDF attachment to ${destination}?`))return;
    setSending(invoice.id);
    try{
      const next=await mutate("sendInvoice",{invoiceId:invoice.id});
      const sent=next.invoices.find(i=>i.id===invoice.id)?.status==="Sent";
      if(!sent)throw new Error("Gmail did not confirm delivery. The invoice remains unsent.");
      flash(`Email and invoice PDF sent to ${destination}`);
    }catch(error){flash(error instanceof Error?error.message:"The email and PDF could not be sent.")}finally{setSending(null)}
  };
  return <div className="page"><div className="page-intro"><div><p className="section-kicker">Phase 3 · Controlled email testing</p><h2>Email centre</h2><p>Review the recipient and message, then send each invoice with its PDF directly through the connected Gmail account.</p></div></div><section className="compose-grid"><article className="panel compose-card"><label>Recipients<select value={programme} onChange={e=>setProgramme(e.target.value)}><option>All</option>{data.programmes.map(p=><option key={p.id}>{p.name}</option>)}</select></label><label>Subject<input value={subject} onChange={e=>setSubject(e.target.value)}/></label><label>Message<textarea value={body} onChange={e=>setBody(e.target.value)}/></label><div className="compose-actions"><span className="safe-label">TEST OUTBOX ONLY</span><button className="primary" disabled={!recipients.length} onClick={prepare}>Prepare {recipients.length} emails</button></div></article><aside className="panel smart-fields"><h3>Personalisation preview</h3><p>Each parent receives a separate record.</p><span>{"{{parent_first_name}}"}</span><span>{"{{player_first_name}}"}</span><span>{"{{programme_name}}"}</span><div className="email-preview"><small>Example</small><p>{body.replaceAll("{{parent_first_name}}",recipients[0]?.payer.trim().split(/\s+/)[0]??"Parent").replaceAll("{{player_first_name}}",recipients[0]?.player.trim().split(/\s+/)[0]??"Player").replaceAll("{{programme_name}}",recipients[0]?.programme??"Programme")}</p></div></aside></section><h3 className="subheading">Test outbox history</h3><section className="email-draft-list">{data.emailOutbox.length===0?<div className="panel invoice-empty"><strong>No prepared emails yet</strong><span>Create an invoice to generate its personalised parent email.</span></div>:data.emailOutbox.slice().reverse().map(e=>{const customer=data.customers.find(c=>c.id===e.customerId),invoice=data.invoices.slice().reverse().find(i=>i.customerId===e.customerId&&e.subject.includes(i.invoiceNumber)),mailto=`mailto:${encodeURIComponent(e.recipient)}?subject=${encodeURIComponent(e.subject)}&body=${encodeURIComponent(e.body)}`,sent=invoice?.status==="Sent";return <article className="panel email-draft-card" key={e.id}><header><div><span className="section-kicker">{e.status}</span><h3>{customer?.payer??"Parent"} · {customer?.player??"Player"}</h3><p><strong>To:</strong> {gmail?.deliveryMode==="test"&&invoice?`${gmail.testRecipient} (test replaces ${e.recipient})`:e.recipient}</p><p><strong>Subject:</strong> {e.subject}</p></div><time>{new Date(e.createdAt).toLocaleString("en-GB")}</time></header><details><summary>Review full personalised draft</summary><pre>{e.body}</pre></details><div className="email-draft-actions">{invoice&&<a className="secondary link-button" href={`/api/invoice?invoiceId=${invoice.id}`} target="_blank" rel="noreferrer">Open invoice PDF</a>}{invoice&&<button className="secondary" disabled={refreshing===invoice.id||sending!==null||sent} onClick={()=>void refresh(invoice.id)}>{refreshing===invoice.id?"Refreshing…":"Refresh draft"}</button>}<button className="secondary" onClick={()=>void copy(e.body)}>Copy message</button>{invoice?<button className="primary" disabled={!gmail||sending!==null||sent} onClick={()=>void send(invoice,e.recipient)}>{sent?"Email + PDF sent":sending===invoice.id?"Sending email + PDF…":!gmail?"Checking Gmail…":!gmail.configured?"Finish Gmail setup":!gmail.connected?"Connect Gmail":"Send email + PDF"}</button>:<a className="primary link-button" href={mailto}>Open in email app</a>}</div><p className="email-reminder">{invoice?sent?"Delivery recorded against this invoice.":gmail?.deliveryMode==="test"?`Safe test: the email and PDF will go only to ${gmail.testRecipient}.`:"Live delivery: the email and PDF will go to the saved parent address.":"General messages still open in your email app for manual sending."}</p></article>})}</section></div>
}

function Settings({data,calendar,mutate,flash}:{data:Snapshot;calendar:boolean;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){const toggle=async()=>{await mutate("setSetting",{key:"calendar_test_connected",value:String(!calendar)});flash(!calendar?"Calendar test connection saved":"Calendar test disconnected")};return <div className="page"><div className="page-intro"><div><p className="section-kicker">Phase 4 · Calendar testing</p><h2>Settings</h2><p>Saved operational rules and integration test controls.</p></div></div><section className="settings-grid"><article className="panel integration-card"><div className="integration-head"><span className="google-g hero">G</span><div><p className="section-kicker">Google Calendar sandbox</p><h3>{calendar?"Test connection active":"Not connected"}</h3></div><span className={`status-pill ${calendar?"paid":"draft"}`}>{calendar?"Saved":"Off"}</span></div><p>This persistent test switch validates calendar behaviour. It does not access a Google account.</p><div className="rule-list"><div><span>✓</span><p><strong>Private by design</strong><small>Personal events will later appear as Busy only.</small></p></div><div><span>✓</span><p><strong>Conflict test</strong><small>The session form blocks the fictional Wednesday clash.</small></p></div><div><span>✓</span><p><strong>Financial separation</strong><small>Calendar changes never alter invoices.</small></p></div></div><button className="primary full" onClick={toggle}>{calendar?"Disconnect test calendar":"Connect test calendar"}</button></article><article className="panel settings-list"><h3>Operational setup</h3><div className="setting-summary"><strong>Venues</strong>{data.venues.map(v=><span key={v.id}>{v.name} · {v.courts} courts</span>)}</div><div className="setting-summary"><strong>Staff</strong>{data.staff.map(s=><span key={s.id}>{s.name} · {s.role}</span>)}</div><div className="setting-summary"><strong>Invoice rules</strong><span>Prefix ST · payment due 1 September 2026</span></div><div className="setting-summary"><strong>Production connections</strong><span>Google OAuth and Stripe remain locked until approved.</span></div></article></section></div>}

function DialogRouter({dialog,close,open,data,termId,mutate,flash}:{dialog:{kind:string;value?:unknown};close:()=>void;open:(dialog:{kind:string;value?:unknown}|null)=>void;data:Snapshot;termId:string;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){const done=async(action:string,payload:unknown,message:string)=>{try{await mutate(action,payload);close();flash(message)}catch(error){flash(error instanceof Error?error.message:"Action failed. Please check the details and try again.")}};
  if(dialog.kind==="customer")return <CustomerForm customer={dialog.value as Customer|undefined} programmes={data.programmes} close={close} save={c=>done(dialog.value?"updateCustomer":"addCustomer",c,dialog.value?"Customer updated":"Customer created and saved")} archive={dialog.value?()=>open({kind:"archiveCustomer",value:dialog.value}):undefined} remove={dialog.value?()=>open({kind:"deleteCustomer",value:dialog.value}):undefined}/>;
  if(dialog.kind==="archiveCustomer"){const c=dialog.value as Customer;return <ConfirmAction title={`Archive ${c.player}?`} kicker="Recommended for former players" copy="This removes the player from active groups, registers, invoices and email selections. Their payment and attendance history is retained." confirm="Archive player" close={close} action={()=>done("archiveCustomer",{id:c.id},`${c.player} archived`)}/>}
  if(dialog.kind==="deleteCustomer"){const c=dialog.value as Customer;return <ConfirmAction title={`Delete ${c.player} permanently?`} kicker="Test records only" copy="This permanently removes the player and all linked enrolments, attendance, invoices, payments, camp bookings and prepared emails. This cannot be undone." confirm="Delete permanently" danger close={close} action={()=>done("deleteCustomer",{id:c.id},`${c.player} permanently deleted`)}/>}
  if(dialog.kind==="clearAll")return <ClearDataConfirm close={close} clear={()=>done("clearAll",{},"All customer and transaction data cleared")}/>;
  if(dialog.kind==="session")return <SessionForm data={data} termId={termId} close={close} save={s=>done("addSession",s,"Session added to the schedule")}/>;
  if(dialog.kind==="substituteCoach")return <SubstituteCoachForm session={dialog.value as Session} staff={data.staff.filter(s=>s.status==="Active")} close={close} save={coach=>done("updateSessionCoach",{id:(dialog.value as Session).id,coach},"Substitute coach assigned to this session only")}/>;
  if(dialog.kind==="term")return <TermForm count={data.terms.length} close={close} save={t=>done("addTerm",t,"New term saved")}/>;
  if(dialog.kind==="editTerm")return <TermForm term={dialog.value as Term} count={data.terms.length} close={close} save={t=>done("updateTerm",t,"Term updated. Invoice choices now match the saved session count")}/>;
  if(dialog.kind==="programme")return <ProgrammeEditor programme={{id:`programme-${Date.now()}`,name:"New Programme",type:"Junior course",weekday:0,startTime:"16:00",venue:data.venues[0]?.name??"",coach:data.staff.find(s=>s.status==="Active")?.name??"Unassigned",capacity:12,pricePence:1200,tone:"blue",status:"Active",minAgeYears:4,maxAgeYears:18,suggestedNextProgrammeId:""}} data={data} close={close} save={p=>done("addProgramme",p,"Programme created")}/>;
  if(dialog.kind==="editProgramme")return <ProgrammeEditor programme={dialog.value as Programme} data={data} close={close} save={p=>done("updateProgramme",p,"Programme updated")}/>;
  if(dialog.kind==="coach")return <CoachForm coach={dialog.value as Staff|undefined} close={close} save={s=>done(dialog.value?"updateStaff":"addStaff",s,dialog.value?"Coach updated":"Coach added")}/>;
  if(dialog.kind==="camp")return <CampForm data={data} close={close} save={c=>done("addCamp",c,"Holiday camp created")}/>;
  if(dialog.kind==="campBooking")return <CampBookingForm camp={dialog.value as Camp} customers={data.customers.filter(c=>c.status!=="Inactive")} close={close} save={p=>done("bookCamp",p,"Camp booking confirmed in test mode")}/>;
  if(dialog.kind==="payment")return <EnhancedPaymentForm data={data} close={close} save={p=>done("recordPayment",p,"Payment recorded and balances updated")}/>;
  if(dialog.kind==="register")return <RegisterForm session={dialog.value as Session} data={data} close={close} registerPlayer={programme=>open({kind:"registerPlayer",value:programme})} save={rows=>done("saveAttendance",{rows},"Attendance register saved")}/>;
  if(dialog.kind==="weeklyRegisters")return <WeeklyRegisters programme={dialog.value as Programme} data={data} termId={termId} close={close} mutate={mutate} flash={flash}/>;
  if(dialog.kind==="players")return <ProgrammeRegister programme={dialog.value as Programme} data={data} termId={termId} close={close} open={open} mutate={mutate} flash={flash}/>;
  if(dialog.kind==="registerPlayer")return <RegisterPlayerForm programme={dialog.value as Programme} data={data} termId={termId} close={close} createCustomer={()=>open({kind:"customerForProgramme",value:dialog.value})} mutate={mutate} flash={flash}/>;
  if(dialog.kind==="trialReview")return <TrialReview enrolment={dialog.value as Enrolment} data={data} close={close} mutate={mutate} flash={flash}/>;
  if(dialog.kind==="termRollover"){const value=dialog.value as {currentTermId:string;nextTermId:string;programmeId?:string};return <TermRolloverReview data={data} {...value} close={close} mutate={mutate} flash={flash}/>;}
  if(dialog.kind==="customerForProgramme"){const programme=dialog.value as Programme;return <CustomerForm initialProgramme={programme.name} programmes={data.programmes} close={close} save={customer=>done("createAndEnrolCustomer",{customer,programmeId:programme.id,termId,status:customer.status},`${customer.player} created and registered for ${programme.name}`)}/>;}
  if(dialog.kind==="campRegister"){const c=dialog.value as Camp,bookings=data.campBookings.filter(b=>b.campName===c.name);return <Modal title={`${c.name} register`} kicker="Saved camp bookings" close={close}><div className="simple-list">{bookings.length?bookings.map(b=><div key={b.id}><span className="mini-avatar">✓</span><p><strong>{data.customers.find(x=>x.id===b.playerId)?.player}</strong><small>{b.bookingOption} · {b.status}</small></p></div>):<p>No test bookings yet.</p>}</div><div className="modal-actions"><button className="primary" onClick={close}>Done</button></div></Modal>}
  return null;
}

function ProgrammeRegister({programme,data,termId,close,open,mutate,flash}:{programme:Programme;data:Snapshot;termId:string;close:()=>void;open:(dialog:{kind:string;value?:unknown}|null)=>void;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){
  const rows=data.enrolments.filter(e=>e.programmeId===programme.id&&e.termId===termId).map(enrolment=>({enrolment,customer:data.customers.find(c=>c.id===enrolment.customerId)})).filter(row=>row.customer) as {enrolment:Enrolment;customer:Customer}[];
  const current=rows.filter(row=>row.enrolment.status!=="Inactive").length,active=rows.filter(row=>row.enrolment.status==="Active").length,trials=rows.filter(row=>row.enrolment.status==="Trial").length;
  const change=async(enrolment:Enrolment,status:string)=>{try{await mutate("updateEnrolmentStatus",{id:enrolment.id,status});flash(`Registration changed to ${status}`)}catch(error){flash(error instanceof Error?error.message:"Registration status could not be changed")}};
  return <Modal title={`${programme.name} register`} kicker="Programme registrations" close={close}><div className="register-summary"><span><small>Current places</small><strong>{current} of {programme.capacity}</strong></span><span><small>Status</small><strong>{active} active · {trials} trial{trials===1?"":"s"}</strong></span></div><div className="simple-list programme-register-list">{rows.length?rows.map(({enrolment,customer})=><div key={enrolment.id}><span className="mini-avatar">{customer.player.split(" ").map(x=>x[0]).join("")}</span><p><strong>{customer.player}</strong><small>{customer.payer} · {customer.email}</small>{enrolment.status==="Trial"&&<small className="trial-progress">Trial {enrolment.trialSessionsCompleted} of {enrolment.trialSessionsAllowed} · {enrolment.trialStatus||"In Progress"}</small>}</p>{enrolment.status==="Trial"?<button className="secondary trial-review-button" onClick={()=>open({kind:"trialReview",value:enrolment})}>Review trial</button>:<select aria-label={`Registration status for ${customer.player}`} value={enrolment.status} onChange={e=>void change(enrolment,e.target.value)}><option>Active</option><option>Inactive</option></select>}</div>):<p className="modal-copy">No players are registered for this programme in the selected term.</p>}</div><div className="modal-actions register-actions"><button className="secondary" onClick={close}>Done</button><button className="primary" onClick={()=>open({kind:"registerPlayer",value:programme})}>+ Add player</button></div></Modal>
}

function RegisterPlayerForm({programme,data,termId,close,createCustomer,mutate,flash}:{programme:Programme;data:Snapshot;termId:string;close:()=>void;createCustomer:()=>void;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){
  const existing=new Map(data.enrolments.filter(e=>e.programmeId===programme.id&&e.termId===termId).map(e=>[e.customerId,e]));
  const available=data.customers.filter(customer=>customer.status!=="Inactive"&&(!existing.has(customer.id)||existing.get(customer.id)?.status==="Inactive"));
  const [customerId,setCustomerId]=useState(available[0]?.id??""),[registrationStatus,setRegistrationStatus]=useState("Trial"),[trialSessionsAllowed,setTrialSessionsAllowed]=useState(1),[nextTrialSession,setNextTrialSession]=useState(""),[saving,setSaving]=useState(false);
  const submit=async()=>{if(!customerId||saving)return;setSaving(true);try{const customer=data.customers.find(c=>c.id===customerId);await mutate("enrolCustomer",{programmeId:programme.id,customerId,termId,status:registrationStatus,trialSessionsAllowed,nextTrialSession});close();flash(`${customer?.player??"Player"} registered for ${programme.name} as ${registrationStatus}`)}catch(error){flash(error instanceof Error?error.message:"Player could not be registered")}finally{setSaving(false)}};
  return <Modal title={`Add player to ${programme.name}`} kicker="Programme registration" close={close}><p className="modal-copy">Select an existing customer. Players already registered in this programme cannot be added twice.</p><form className="form-grid" onSubmit={e=>{e.preventDefault();void submit()}}><Field label="Existing customer"><select required value={customerId} onChange={e=>setCustomerId(e.target.value)}><option value="">Choose a player</option>{available.map(customer=><option key={customer.id} value={customer.id}>{customer.player} · {customer.payer}</option>)}</select></Field><Field label="Registration status"><select value={registrationStatus} onChange={e=>setRegistrationStatus(e.target.value)}><option>Trial</option><option>Active</option></select></Field>{registrationStatus==="Trial"&&<><Field label="Free trial sessions"><select value={trialSessionsAllowed} onChange={e=>setTrialSessionsAllowed(+e.target.value)}><option value={1}>1 session</option><option value={2}>2 sessions</option><option value={3}>3 sessions</option><option value={4}>4 sessions</option></select></Field><Field label="Next trial session"><input type="date" value={nextTrialSession} onChange={e=>setNextTrialSession(e.target.value)}/></Field></>}<div className="registration-note wide"><strong>{available.length?`${available.length} customer${available.length===1?"":"s"} available`:`Every current customer is already registered in ${programme.name}`}</strong><span>Trial players appear on the register but are excluded from invoices until changed to Active.</span></div><div className="modal-actions wide register-actions"><button type="button" className="secondary" onClick={createCustomer}>+ Create new customer</button><button type="button" className="secondary" onClick={close}>Cancel</button><button className="primary" disabled={!customerId||saving}>{saving?"Registering…":"Register player"}</button></div></form></Modal>
}

function TrialReview({enrolment,data,close,mutate,flash}:{enrolment:Enrolment;data:Snapshot;close:()=>void;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){
  const current=data.enrolments.find(e=>e.id===enrolment.id)??enrolment,customer=data.customers.find(c=>c.id===current.customerId),programme=data.programmes.find(p=>p.id===current.programmeId),[saving,setSaving]=useState(false);
  const act=async(trialAction:string,message:string)=>{if(saving)return;setSaving(true);try{await mutate("updateTrial",{enrolmentId:current.id,trialAction});flash(message);if(["makeActive","notJoining"].includes(trialAction))close()}catch(error){flash(error instanceof Error?error.message:"Trial could not be updated")}finally{setSaving(false)}};
  const complete=current.trialSessionsCompleted>=current.trialSessionsAllowed;
  return <Modal title={`${customer?.player||"Player"} free trial`} kicker={programme?.name||"Trial review"} close={close}><div className="trial-review"><div className="trial-count"><strong>{current.trialSessionsCompleted}</strong><span>of {current.trialSessionsAllowed} sessions completed</span></div><div className="trial-meter"><i style={{width:`${Math.min(100,current.trialSessionsCompleted/Math.max(1,current.trialSessionsAllowed)*100)}%`}}/></div><dl><div><dt>Status</dt><dd><em className={`traffic ${complete?"green":"amber"}`}>{current.trialStatus||"In Progress"}</em></dd></div><div><dt>Next session</dt><dd>{current.nextTrialSession||"Not scheduled"}</dd></div><div><dt>Invoice eligibility</dt><dd>Excluded until made Active</dd></div></dl></div><div className="trial-actions"><button className="secondary" disabled={saving||complete} onClick={()=>void act("completeSession","Trial attendance recorded")}>Complete trial session</button><button className="secondary" disabled={saving} onClick={()=>void act("extend","Trial extended by one session")}>Extend by 1 session</button><button className="danger-link" disabled={saving} onClick={()=>void act("notJoining","Trial closed as not joining")}>Not joining</button><button className="primary" disabled={saving||!complete} title={complete?"":"Complete the allowed trial sessions first"} onClick={()=>void act("makeActive",`${customer?.player||"Player"} made Active and is now invoice eligible`)}>Make Active</button></div></Modal>
}

function TermRolloverReview({data,currentTermId,nextTermId,programmeId,close,mutate,flash}:{data:Snapshot;currentTermId:string;nextTermId:string;programmeId?:string;close:()=>void;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){
  const currentTerm=data.terms.find(t=>t.id===currentTermId),nextTerm=data.terms.find(t=>t.id===nextTermId),rollover=data.termRollovers.find(r=>r.currentTermId===currentTermId&&r.nextTermId===nextTermId),allRows=data.rolloverDecisions.filter(d=>d.rolloverId===rollover?.id),rows=programmeId?allRows.filter(d=>d.currentProgrammeId===programmeId):allRows,[saving,setSaving]=useState(false);
  const prepare=async()=>{setSaving(true);try{await mutate("prepareTermRollover",{currentTermId,nextTermId});flash(`${nextTerm?.name} review prepared`)}catch(error){flash(error instanceof Error?error.message:"Next term could not be prepared")}finally{setSaving(false)}};
  const update=async(row:RolloverDecision,changes:Partial<RolloverDecision>)=>{setSaving(true);try{await mutate("updateRolloverDecision",{...row,...changes});flash("Next-term decision saved")}catch(error){flash(error instanceof Error?error.message:"Decision could not be saved")}finally{setSaving(false)}};
  const complete=async()=>{if(!rollover)return;setSaving(true);try{await mutate("completeTermRollover",{rolloverId:rollover.id});flash(`${nextTerm?.name} enrolments created. No invoices were sent.`);close()}catch(error){flash(error instanceof Error?error.message:"Rollover could not be completed")}finally{setSaving(false)}};
  const excluded=data.enrolments.filter(e=>e.termId===currentTermId&&e.status!=="Active").length,unresolved=allRows.filter(r=>r.continuationStatus==="Awaiting Confirmation"||["Recommended","Review Later"].includes(r.progressionStatus)).length;
  return <Modal title={`Review ${nextTerm?.name||"Next Term"}`} kicker={`${currentTerm?.name||"Current term"} → ${nextTerm?.name||"Next term"}`} close={close}>{!rollover?<><div className="rollover-intro"><span><strong>{data.enrolments.filter(e=>e.termId===currentTermId&&e.status==="Active").length}</strong><small>active players to review</small></span><span><strong>{excluded}</strong><small>trial or inactive players excluded</small></span><span><strong>0</strong><small>invoices created or sent</small></span></div><p className="modal-copy">Preparing creates a review list only. You decide who continues and resolve any age-based progression reviews before next-term enrolments are created.</p><div className="modal-actions"><button className="secondary" onClick={close}>Cancel</button><button className="primary" disabled={saving} onClick={()=>void prepare()}>{saving?"Preparing…":"Prepare Next Term"}</button></div></>:<><div className="rollover-summary"><span><small>Continuing</small><strong>{allRows.filter(r=>r.continuationStatus==="Continuing").length}</strong></span><span><small>Not continuing</small><strong>{allRows.filter(r=>r.continuationStatus==="Not Continuing").length}</strong></span><span><small>Awaiting</small><strong>{allRows.filter(r=>r.continuationStatus==="Awaiting Confirmation").length}</strong></span><span><small>Progression reviews</small><strong>{allRows.filter(r=>["Recommended","Review Later"].includes(r.progressionStatus)).length}</strong></span></div><div className="rollover-list">{rows.map(row=>{const customer=data.customers.find(c=>c.id===row.customerId),programme=data.programmes.find(p=>p.id===row.currentProgrammeId),suggested=data.programmes.find(p=>p.id===programme?.suggestedNextProgrammeId),age=row.ageAtNextTermMonths===null?null:(row.ageAtNextTermMonths/12).toFixed(1);return <article key={row.id} className={row.progressionStatus==="Recommended"||row.progressionStatus==="Review Later"?"needs-review":""}><div><strong>{customer?.player}</strong><small>{programme?.name} · {customer?.dateOfBirth?`Age ${age} at term start`:"DOB missing"}</small></div><label>Continuation<select disabled={saving} value={row.continuationStatus} onChange={e=>void update(row,{continuationStatus:e.target.value})}><option>Awaiting Confirmation</option><option>Continuing</option><option>Not Continuing</option></select></label>{row.progressionStatus!=="Not Required"&&<div className="progression-choice"><span><strong>Progression review</strong><small>{suggested?`Suggested next stage: ${suggested.name}`:"Age is outside this programme’s recommended range"}</small></span><select disabled={saving} value={row.progressionStatus} onChange={e=>{const value=e.target.value;void update(row,{progressionStatus:value,nextProgrammeId:value==="Move Next Term"?(suggested?.id||row.nextProgrammeId):row.currentProgrammeId})}}><option>Recommended</option><option>Move Next Term</option><option>Keep Current Group</option><option>Review Later</option></select>{row.progressionStatus==="Move Next Term"&&<select value={row.nextProgrammeId} onChange={e=>void update(row,{nextProgrammeId:e.target.value})}>{data.programmes.filter(p=>p.status==="Active").map(p=><option key={p.id} value={p.id}>{p.name} · {p.venue}</option>)}</select>}</div>}</article>})}</div><div className="rollover-safety"><strong>No invoices will be created or sent.</strong><span>Completion only creates the confirmed next-term registrations. Duplicate enrolments are prevented.</span></div><div className="modal-actions"><button className="secondary" onClick={close}>Close</button><button className="primary" disabled={saving||unresolved>0||rollover.status==="Complete"} title={unresolved?`Resolve ${unresolved} decisions first`:""} onClick={()=>void complete()}>{rollover.status==="Complete"?"Rollover complete":unresolved?`${unresolved} decisions remaining`:"Create Next-Term Enrolments"}</button></div></>}</Modal>
}

function CustomerForm({customer,initialProgramme,programmes,close,save,archive,remove}:{customer?:Customer;initialProgramme?:string;programmes:Programme[];close:()=>void;save:(c:Customer)=>Promise<void>;archive?:()=>void;remove?:()=>void}){
  const [c,setC]=useState<Customer>(customer??{id:`ST-${Date.now().toString().slice(-7)}${Math.floor(10+Math.random()*90)}`,player:"",payer:"",email:"",phone:"",programme:initialProgramme??programmes[0]?.name??"Mini Red",balance:"£0.00",payment:"Not invoiced",status:"Trial",medical:"No medical needs recorded",emergency:"",consent:"Pending",dateOfBirth:"",createdAt:""}),[saving,setSaving]=useState(false);
  const field=(k:keyof Customer,v:string)=>setC({...c,[k]:v});
  const submit=async()=>{if(saving)return;setSaving(true);try{await save(c)}finally{setSaving(false)}};
  return <Modal title={customer?`Edit ${customer.player}`:"Add customer and player"} kicker="Operational record" close={close}><form className="form-grid" onSubmit={e=>{e.preventDefault();void submit()}}><Field label="Player name"><input required value={c.player} onChange={e=>field("player",e.target.value)}/></Field><Field label="Status"><select value={c.status} onChange={e=>field("status",e.target.value)}><option>Trial</option><option>Active</option><option>Inactive</option></select></Field><Field label="Date of birth"><input type="date" value={c.dateOfBirth} onChange={e=>field("dateOfBirth",e.target.value)}/></Field><Field label="Parent or payer"><input required value={c.payer} onChange={e=>field("payer",e.target.value)}/></Field><Field label="Email"><input required type="email" value={c.email} onChange={e=>field("email",e.target.value)}/></Field><Field label="Phone"><input required value={c.phone} onChange={e=>field("phone",e.target.value)}/></Field><Field label="Programme"><select value={c.programme} onChange={e=>field("programme",e.target.value)}>{programmes.map(p=><option key={p.id}>{p.name}</option>)}</select></Field><Field label="Emergency contact"><input value={c.emergency} onChange={e=>field("emergency",e.target.value)}/></Field><Field label="Consent"><select value={c.consent} onChange={e=>field("consent",e.target.value)}><option>Pending</option><option>Complete</option><option>Declined</option></select></Field><label className="wide">Medical or additional needs<textarea value={c.medical} onChange={e=>field("medical",e.target.value)}/></label>{customer&&<div className="record-management wide"><div><strong>Player no longer attending?</strong><span>Archive is the safe option. Permanent deletion is only for test records entered by mistake.</span></div><div><button type="button" className="secondary" onClick={archive}>Archive player</button><button type="button" className="danger-link" onClick={remove}>Delete permanently</button></div></div>}<Actions close={close} label={saving?"Saving…":customer?"Save changes":"Create record"} disabled={saving}/></form></Modal>
}

function ConfirmAction({title,kicker,copy,confirm,danger=false,close,action}:{title:string;kicker:string;copy:string;confirm:string;danger?:boolean;close:()=>void;action:()=>void}){return <Modal title={title} kicker={kicker} close={close}><p className="modal-copy">{copy}</p><div className="modal-actions"><button className="secondary" onClick={close}>Cancel</button><button className={danger?"danger-button":"primary"} onClick={action}>{confirm}</button></div></Modal>}

function ClearDataConfirm({close,clear}:{close:()=>void;clear:()=>void}){const [confirmation,setConfirmation]=useState("");return <Modal title="Clear all customer data?" kicker="Operational test version" close={close}><p className="modal-copy">This permanently removes every customer, enrolment, attendance mark, invoice, payment, camp booking and prepared email. Terms, programmes, sessions, camps, venues and staff are kept so the system remains usable.</p><label className="confirm-field">Type <strong>CLEAR</strong> to continue<input value={confirmation} onChange={e=>setConfirmation(e.target.value)} autoCapitalize="characters"/></label><div className="modal-actions"><button className="secondary" onClick={close}>Cancel</button><button className="danger-button" disabled={confirmation.trim().toUpperCase()!=="CLEAR"} onClick={clear}>Clear all data</button></div></Modal>}

function SessionForm({data,termId,close,save}:{data:Snapshot;termId:string;close:()=>void;save:(s:Omit<Session,"id">)=>void}){const [title,setTitle]=useState(data.programmes[0]?.name??"Mini Red"),[day,setDay]=useState(2),[start,setStart]=useState("16:30"),[duration,setDuration]=useState(60),[venue,setVenue]=useState(data.venues[0]?.name??"Warsash Tennis Club"),[coach,setCoach]=useState(data.staff[0]?.name??"Jake"),[date,setDate]=useState("2026-08-19");const clash=day===2&&start>="10:30"&&start<="12:00";return <Modal title="Add coaching session" kicker="Schedule" close={close}><form className="form-grid" onSubmit={e=>{e.preventDefault();if(!clash)save({day,start,title,meta:`${data.enrolments.filter(x=>data.programmes.find(p=>p.id===x.programmeId)?.name===title).length} players`,venue,coach,tone:data.programmes.find(p=>p.name===title)?.tone??"blue",termId,duration,sessionDate:date})}}><Field label="Programme"><select value={title} onChange={e=>setTitle(e.target.value)}>{data.programmes.map(p=><option key={p.id}>{p.name}</option>)}</select></Field><Field label="Date"><input type="date" value={date} onChange={e=>{setDate(e.target.value);setDay((new Date(e.target.value+"T12:00:00Z").getUTCDay()+6)%7)}}/></Field><Field label="Start"><input type="time" value={start} onChange={e=>setStart(e.target.value)}/></Field><Field label="Duration"><select value={duration} onChange={e=>setDuration(+e.target.value)}><option value="45">45 minutes</option><option value="60">60 minutes</option><option value="90">90 minutes</option></select></Field><Field label="Venue"><select value={venue} onChange={e=>setVenue(e.target.value)}>{data.venues.map(v=><option key={v.id}>{v.name}</option>)}</select></Field><Field label="Coach"><select value={coach} onChange={e=>setCoach(e.target.value)}>{data.staff.map(s=><option key={s.id}>{s.name}</option>)}</select></Field>{clash&&<div className="conflict wide"><strong>Test calendar conflict</strong><span>The fictional diary is busy between 10:30 and 12:00 on Wednesday.</span></div>}<Actions close={close} label="Save session" disabled={clash}/></form></Modal>}

function WeeklyRegisters({programme,data,termId,close,mutate,flash}:{programme:Programme;data:Snapshot;termId:string;close:()=>void;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){
  const [expanded,setExpanded]=useState<number|null>(null),[working,setWorking]=useState(false);
  const term=data.terms.find(row=>row.id===termId),weeks=data.programmeRegisters.filter(row=>row.programmeId===programme.id&&row.termId===termId).sort((a,b)=>a.weekNumber-b.weekNumber);
  const groupEnrolments=data.enrolments.filter(row=>row.programmeId===programme.id&&row.termId===termId&&row.status!=="Inactive"),players=data.customers.filter(customer=>groupEnrolments.some(row=>row.customerId===customer.id)&&customer.status!=="Inactive");
  const prepare=async()=>{if(working)return;setWorking(true);try{const next=await mutate("prepareProgrammeRegisters",{programmeId:programme.id,termId});const first=next.programmeRegisters.find(row=>row.programmeId===programme.id&&row.termId===termId);setExpanded(first?.id??null);flash(`${term?.sessionCount??0} weekly registers prepared for ${programme.name}`)}catch(error){flash(error instanceof Error?error.message:"Registers could not be prepared")}finally{setWorking(false)}};
  const completed=weeks.filter(row=>row.status==="Complete").length;
  return <Modal title={`${programme.name} weekly registers`} kicker={`${term?.name??"Selected term"} · ${players.length} player${players.length===1?"":"s"}`} close={close}><div className="register-summary"><span><small>Term weeks</small><strong>{weeks.length||term?.sessionCount||0}</strong></span><span><small>Complete</small><strong>{completed}</strong></span><span><small>Remaining</small><strong>{Math.max(0,(weeks.length||term?.sessionCount||0)-completed)}</strong></span></div>{!weeks.length?<div className="prepare-registers"><strong>Prepare the term register</strong><p>This creates one expandable attendance list for every scheduled week. It does not change the schedule or invoices.</p><button className="primary" disabled={working} onClick={()=>void prepare()}>{working?"Preparing weeks…":`Prepare ${term?.sessionCount??0} weekly registers`}</button></div>:<div className="weekly-register-list">{weeks.map(week=><WeeklyRegisterWeek key={week.id} week={week} open={expanded===week.id} toggle={()=>setExpanded(expanded===week.id?null:week.id)} players={players} marks={data.programmeRegisterMarks.filter(mark=>mark.registerId===week.id)} working={working} save={async rows=>{setWorking(true);try{await mutate("saveProgrammeRegister",{registerId:week.id,rows});flash(`Week ${week.weekNumber} register saved`)}catch(error){flash(error instanceof Error?error.message:"Register could not be saved")}finally{setWorking(false)}}}/>)}</div>}<div className="modal-actions"><button className="primary" onClick={close}>Done</button></div></Modal>
}

function WeeklyRegisterWeek({week,open,toggle,players,marks,save,working}:{week:ProgrammeRegister;open:boolean;toggle:()=>void;players:Customer[];marks:ProgrammeRegisterMark[];save:(rows:{playerId:string;mark:string;note:string}[])=>void;working:boolean}){
  const [values,setValues]=useState<Record<string,string>>(Object.fromEntries(players.map(player=>[player.id,marks.find(mark=>mark.playerId===player.id)?.mark??"Present"])));
  useEffect(()=>setValues(Object.fromEntries(players.map(player=>[player.id,marks.find(mark=>mark.playerId===player.id)?.mark??"Present"]))),[marks.length,players.length]);
  const attended=marks.filter(mark=>["Present","Late"].includes(mark.mark)).length;
  return <section className={`weekly-register-week ${week.status==="Complete"?"complete":""}`}><button className="weekly-register-heading" onClick={toggle} aria-expanded={open}><span><b>Week {week.weekNumber}</b><small>{new Date(`${week.sessionDate}T12:00:00Z`).toLocaleDateString("en-GB",{weekday:"short",day:"numeric",month:"short",year:"numeric"})}</small></span><span><em className={`traffic ${week.status==="Complete"?"green":"amber"}`}>{week.status}</em><small>{week.status==="Complete"?`${attended} attended`:"Open to complete"}</small></span><b>{open?"−":"+"}</b></button>{open&&<div className="weekly-register-body">{players.map(player=><div className="weekly-player" key={player.id}><span className="mini-avatar">{player.player.split(" ").map(part=>part[0]).join("")}</span><strong>{player.player}</strong><div className="attendance-buttons">{["Present","Late","Absent","Excused"].map(mark=><button key={mark} type="button" className={values[player.id]===mark?"selected":""} onClick={()=>setValues({...values,[player.id]:mark})}>{mark}</button>)}</div></div>)}{!players.length&&<p className="modal-copy">No active or trial players are registered for this term.</p>}<button className="primary save-week" disabled={working||!players.length} onClick={()=>save(players.map(player=>({playerId:player.id,mark:values[player.id]||"Present",note:""})))}>{working?"Saving…":"Save this week’s register"}</button></div>}</section>
}

function RegisterForm({session,data,close,registerPlayer,save}:{session:Session;data:Snapshot;close:()=>void;registerPlayer:(programme:Programme)=>void;save:(r:Omit<Attendance,"id">[])=>void}){const programme=data.programmes.find(p=>p.name===session.title),registrations=data.enrolments.filter(e=>e.programmeId===programme?.id&&e.termId===session.termId&&e.status!=="Inactive"),ids=registrations.map(e=>e.customerId),players=data.customers.filter(c=>ids.includes(c.id)&&c.status!=="Inactive"),existing=Object.fromEntries(data.attendance.filter(a=>a.sessionId===session.id).map(a=>[a.playerId,a.mark])),[marks,setMarks]=useState<Record<string,string>>(Object.fromEntries(players.map(p=>[p.id,existing[p.id]??"Present"])));return <Modal title={`${session.title} register`} kicker={`${session.sessionDate} · ${session.start}`} close={close}><div className="register-list">{players.map(p=>{const status=registrations.find(e=>e.customerId===p.id)?.status;return <div key={p.id}><span className="mini-avatar">{p.player.split(" ").map(x=>x[0]).join("")}</span><p><strong>{p.player} <em className={`registration-status ${status?.toLowerCase()}`}>{status}</em></strong>{p.medical!=="No medical needs recorded"&&<small className="medical-flag">{p.medical}</small>}</p><div className="attendance-buttons">{["Present","Late","Absent"].map(m=><button key={m} type="button" className={marks[p.id]===m?"selected":""} onClick={()=>setMarks({...marks,[p.id]:m})}>{m}</button>)}</div></div>})}</div>{!players.length&&<p className="modal-copy">No players are registered in this programme for the selected term.</p>}<div className="modal-actions register-actions">{programme&&<button className="secondary" onClick={()=>registerPlayer(programme)}>+ Register player</button>}<button className="secondary" onClick={close}>Cancel</button><button className="primary" disabled={!players.length} onClick={()=>save(players.map(p=>({sessionId:session.id,playerId:p.id,mark:marks[p.id],attendedOn:session.sessionDate})))}>Save register</button></div></Modal>}

function TermForm({term,count,close,save}:{term?:Term;count:number;close:()=>void;save:(t:Term)=>void}){const [name,setName]=useState(term?.name??"Autumn Term 2027"),[start,setStart]=useState(term?.startDate??"2027-09-06"),[end,setEnd]=useState(term?.endDate??"2027-12-17"),[sessions,setSessions]=useState(term?.sessionCount??10);return <Modal title={term?"Edit term":"Add a term"} kicker="Academic sequence" close={close}><form className="form-grid" onSubmit={e=>{e.preventDefault();save({id:term?.id??name.toLowerCase().replace(/[^a-z0-9]+/g,"-")+Date.now(),name,startDate:start,endDate:end,sessionCount:Math.max(1,Math.min(14,sessions)),status:term?.status??"Planned",sortOrder:term?.sortOrder??count+1})}}><Field label="Term name"><input value={name} onChange={e=>setName(e.target.value)} required/></Field><Field label="Sessions in this term"><input type="number" min="1" max="14" value={sessions} onChange={e=>setSessions(+e.target.value)} required/><small>Choose between 1 and 14. Invoice adjustments and weekly registers will use this number.</small></Field><Field label="Start date"><input type="date" value={start} onChange={e=>setStart(e.target.value)} required/></Field><Field label="End date"><input type="date" value={end} onChange={e=>setEnd(e.target.value)} required/></Field><div className="term-sequence wide"><strong>Sequence</strong><span>Autumn → Winter optional → Spring → Summer → next Autumn</span></div><Actions close={close} label={term?"Save term changes":"Save term"}/></form></Modal>}

function ProgrammeForm({data,close,save}:{data:Snapshot;close:()=>void;save:(p:Programme)=>void}){const [name,setName]=useState("New Junior Group"),[venue,setVenue]=useState(data.venues[0]?.name??""),[coach,setCoach]=useState(data.staff[0]?.name??""),[capacity,setCapacity]=useState(12),[price,setPrice]=useState(12);return <Modal title="Create programme" kicker="Term coaching" close={close}><form className="form-grid" onSubmit={e=>{e.preventDefault();save({id:name.toLowerCase().replace(/[^a-z0-9]+/g,"-")+Date.now(),name,type:"Junior course",weekday:1,startTime:"16:00",venue,coach,capacity,pricePence:price*100,tone:"blue",status:"Active",minAgeYears:4,maxAgeYears:18,suggestedNextProgrammeId:""})}}><Field label="Name"><input value={name} onChange={e=>setName(e.target.value)}/></Field><Field label="Capacity"><input type="number" value={capacity} onChange={e=>setCapacity(+e.target.value)}/></Field><Field label="Venue"><select value={venue} onChange={e=>setVenue(e.target.value)}>{data.venues.map(v=><option key={v.id}>{v.name}</option>)}</select></Field><Field label="Coach"><select value={coach} onChange={e=>setCoach(e.target.value)}>{data.staff.map(s=><option key={s.id}>{s.name}</option>)}</select></Field><Field label="Price per session"><input type="number" value={price} onChange={e=>setPrice(+e.target.value)}/></Field><Actions close={close} label="Create programme"/></form></Modal>}

function CampForm({data,close,save}:{data:Snapshot;close:()=>void;save:(c:Camp)=>void}){const [name,setName]=useState("Easter Holiday Camp"),[start,setStart]=useState("2027-04-06"),[end,setEnd]=useState("2027-04-08"),[venue,setVenue]=useState(data.venues[0]?.name??""),[capacity,setCapacity]=useState(24);return <Modal title="Create holiday camp" kicker="One-off programme" close={close}><form className="form-grid" onSubmit={e=>{e.preventDefault();save({id:name.toLowerCase().replace(/[^a-z0-9]+/g,"-")+Date.now(),name,startDate:start,endDate:end,venue,time:"09:00–15:00",capacity,dayPricePence:3500,fullPricePence:9500,status:"Draft"})}}><Field label="Camp name"><input value={name} onChange={e=>setName(e.target.value)}/></Field><Field label="Capacity"><input type="number" value={capacity} onChange={e=>setCapacity(+e.target.value)}/></Field><Field label="Start"><input type="date" value={start} onChange={e=>setStart(e.target.value)}/></Field><Field label="End"><input type="date" value={end} onChange={e=>setEnd(e.target.value)}/></Field><Field label="Venue"><select value={venue} onChange={e=>setVenue(e.target.value)}>{data.venues.map(v=><option key={v.id}>{v.name}</option>)}</select></Field><Actions close={close} label="Create camp"/></form></Modal>}

function CampBookingForm({camp,customers,close,save}:{camp:Camp;customers:Customer[];close:()=>void;save:(p:Omit<CampBooking,"id"|"createdAt">)=>void}){const [playerId,setPlayerId]=useState(customers[0]?.id??""),[full,setFull]=useState(true),[method,setMethod]=useState("Test card");return <Modal title={`Book ${camp.name}`} kicker="Safe test booking" close={close}><form className="form-grid" onSubmit={e=>{e.preventDefault();save({campName:camp.name,playerId,bookingOption:full?"Full camp":"Single day",amountPence:full?camp.fullPricePence:camp.dayPricePence,paymentMethod:method,status:"Test confirmed"})}}><Field label="Player"><select value={playerId} onChange={e=>setPlayerId(e.target.value)}>{customers.map(c=><option value={c.id} key={c.id}>{c.player}</option>)}</select></Field><Field label="Option"><select value={full?"full":"day"} onChange={e=>setFull(e.target.value==="full")}><option value="full">Full camp · {money(camp.fullPricePence)}</option><option value="day">Single day · {money(camp.dayPricePence)}</option></select></Field><Field label="Payment test"><select value={method} onChange={e=>setMethod(e.target.value)}><option>Test card</option><option>Test invoice</option><option>Complimentary</option></select></Field><div className="booking-summary wide"><span><small>Total</small><strong>{money(full?camp.fullPricePence:camp.dayPricePence)}</strong></span><span><small>Mode</small><strong>No money taken</strong></span></div><Actions close={close} label="Confirm test booking"/></form></Modal>}

function EnhancedPaymentForm({data,close,save}:{data:Snapshot;close:()=>void;save:(p:unknown)=>void}){const unpaid=data.invoices.filter(i=>i.lifecycleStatus==="Active"&&balanceForInvoice(data,i)>0),[invoiceId,setInvoiceId]=useState(unpaid[0]?.id??0),invoice=data.invoices.find(i=>i.id===invoiceId),[amount,setAmount]=useState(invoice?balanceForInvoice(data,invoice):0),[method,setMethod]=useState("Bank transfer"),[paidAt,setPaidAt]=useState("2026-08-20"),[notes,setNotes]=useState("");useEffect(()=>{const i=data.invoices.find(x=>x.id===invoiceId);if(i)setAmount(balanceForInvoice(data,i))},[invoiceId]);const remaining=invoice?balanceForInvoice(data,invoice):0;return <Modal title="Record Payment" kicker="Payment reconciliation" close={close}><form className="form-grid" onSubmit={e=>{e.preventDefault();if(invoice&&amount>0&&amount<=remaining)save({invoiceId,customerId:invoice.customerId,amountPence:amount,method,paidAt,notes})}}><Field label="Invoice"><select value={invoiceId} onChange={e=>setInvoiceId(+e.target.value)}>{unpaid.map(i=><option key={i.id} value={i.id}>{i.invoiceNumber} · {data.customers.find(c=>c.id===i.customerId)?.player} · {money(balanceForInvoice(data,i))} due</option>)}</select></Field><Field label="Payment date"><input type="date" value={paidAt} onChange={e=>setPaidAt(e.target.value)}/></Field><Field label="Amount received"><input type="number" min="0.01" max={remaining/100} step="0.01" value={amount/100} onChange={e=>setAmount(Math.round(+e.target.value*100))}/></Field><Field label="Payment method"><select value={method} onChange={e=>setMethod(e.target.value)}><option>Bank transfer</option><option>Card</option><option>Cash</option><option>Other</option></select></Field><label className="wide">Notes<textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Optional reference or note"/></label>{invoice&&<div className="payment-match wide"><span><small>Invoice total</small><strong>{money(invoice.amountPence)}</strong></span><span><small>Already paid</small><strong>{money(paidForInvoice(data,invoice.id))}</strong></span><span><small>Balance before this payment</small><strong>{money(remaining)}</strong></span><span><small>Status after saving</small><strong>{amount>=remaining?"Paid":"Part Paid"}</strong></span></div>}<Actions close={close} label="Record Payment" disabled={!invoice||amount<=0||amount>remaining||!paidAt}/></form></Modal>}

function Field({label,children}:{label:string;children:ReactNode}){return <label>{label}{children}</label>}
function Actions({close,label,disabled=false}:{close:()=>void;label:string;disabled?:boolean}){return <div className="modal-actions wide"><button type="button" className="secondary" onClick={close}>Cancel</button><button className="primary" disabled={disabled}>{label}</button></div>}
function Modal({title,kicker,close,children}:{title:string;kicker:string;close:()=>void;children:ReactNode}){return <div className="modal-wrap"><button className="modal-scrim" onClick={close}/><div className="confirm-modal wide-modal"><div className="modal-title"><div><p className="section-kicker">{kicker}</p><h2>{title}</h2></div><button onClick={close}>×</button></div>{children}</div></div>}

function VenueProgrammes({data,termId,open,goInvoices,initialTab="terms"}:{data:Snapshot;termId:string;open:(v:{kind:string;value?:unknown})=>void;goInvoices:(programmeId:string)=>void;initialTab?:"terms"|"camps"}){const [tab,setTab]=useState<"terms"|"camps">(initialTab),[closed,setClosed]=useState<string[]>([]);const toggle=(venue:string)=>setClosed(current=>current.includes(venue)?current.filter(v=>v!==venue):[...current,venue]);return <div className="page"><div className="page-intro"><div><h2>{tab==="terms"?"Programmes by venue":"Holiday camps"}</h2><p>{tab==="terms"?"Open a venue to see programmes, registrations and invoice actions.":"Manage bookings, capacity and camp registers in one place."}</p></div><button className="primary" onClick={()=>open({kind:tab==="terms"?"programme":"camp"})}>+ New {tab==="terms"?"programme":"camp"}</button></div><div className="programme-tabs"><button className={tab==="terms"?"active":""} onClick={()=>setTab("terms")}>Term programmes</button><button className={tab==="camps"?"active":""} onClick={()=>setTab("camps")}>Holiday camps <span>{data.camps.length}</span></button></div>{tab==="terms"?<div className="venue-programme-list">{data.venues.map(venue=>{const rows=data.programmes.filter(p=>p.venue===venue.name),isClosed=closed.includes(venue.name);return <section className="venue-section panel" key={venue.id}><button className="venue-heading" onClick={()=>toggle(venue.name)} aria-expanded={!isClosed}><span className="venue-pin"><AppIcon name="venue"/></span><span><strong>{venue.name}</strong><small>{rows.length} programme{rows.length===1?"":"s"} · {venue.courts} courts</small></span><b>{isClosed?"+":"−"}</b></button>{!isClosed&&<div className="programme-grid venue-programme-grid">{rows.map(p=><ProgrammeCard key={p.id} p={p} data={data} termId={termId} open={open} goInvoices={goInvoices}/>)}</div>}</section>})}</div>:<div className="camp-list">{data.camps.map(c=>{const booked=data.campBookings.filter(b=>b.campName===c.name).length;return <article className="panel camp-card" key={c.id}><div className="camp-date"><span>{new Date(c.startDate+"T12:00:00Z").getUTCDate()}</span><small>{new Date(c.startDate+"T12:00:00Z").toLocaleDateString("en-GB",{month:"short",year:"numeric"})}</small></div><div className="camp-main"><span className={`status-pill ${c.status==="Open"?"paid":"draft"}`}>{c.status}</span><h3>{c.name}</h3><p>{c.venue} · {c.time}</p><div className="camp-numbers"><span><small>Bookings</small><strong>{booked} / {c.capacity}</strong></span><span><small>Price</small><strong>{money(c.dayPricePence)} day / {money(c.fullPricePence)} full</strong></span></div></div><div className="camp-actions"><button className="secondary" onClick={()=>open({kind:"campRegister",value:c})}>View register</button><button className="primary" onClick={()=>open({kind:"campBooking",value:c})}>+ Test booking</button></div></article>})}</div>}</div>}

function ProgrammeCard({p,data,termId,open,goInvoices}:{p:Programme;data:Snapshot;termId:string;open:(v:{kind:string;value?:unknown})=>void;goInvoices:(id:string)=>void}){const registrations=data.enrolments.filter(e=>e.programmeId===p.id&&e.termId===termId&&e.status!=="Inactive"),active=registrations.filter(e=>e.status==="Active").length,trials=registrations.filter(e=>e.status==="Trial").length,percent=p.capacity?Math.round(registrations.length/p.capacity*100):0,current=data.terms.find(t=>t.id===termId),next=data.terms.filter(t=>t.sortOrder>(current?.sortOrder??0)).sort((a,b)=>a.sortOrder-b.sortOrder)[0],weeks=data.programmeRegisters.filter(row=>row.programmeId===p.id&&row.termId===termId),complete=weeks.filter(row=>row.status==="Complete").length;return <article className="programme-card"><span className={`programme-tone ${p.tone}`}/><div className="programme-title"><div><span className="programme-type">{p.type||"Junior course"}</span><h3>{p.name}</h3><p>{["Mon","Tue","Wed","Thu","Fri","Sat","Sun"][p.weekday]} · {p.startTime} · {money(p.pricePence)} per session</p></div><button className="edit-link" onClick={()=>open({kind:"editProgramme",value:p})}>Edit</button></div><div className="programme-meta"><span><small>Coach</small><strong className={!p.coach||p.coach==="Unassigned"?"text-red":""}>{p.coach||"Coach required"}</strong></span><span><small>Capacity</small><strong>{registrations.length} of {p.capacity}</strong></span></div><div className="capacity-row"><span>{active} active{trials?` · ${trials} trial${trials===1?"":"s"}`:""}</span><b>{percent}%</b></div><div className="progress"><i style={{width:`${Math.min(100,percent)}%`}}/></div><div className="programme-register-status"><span className={`traffic ${weeks.length&&complete===weeks.length?"green":"amber"}`}>{weeks.length?`${complete} of ${weeks.length} registers complete`:"Registers not prepared"}</span></div><div className="card-actions programme-actions"><button className="secondary" onClick={()=>open({kind:"weeklyRegisters",value:p})}>Weekly registers</button><button className="secondary" onClick={()=>open({kind:"registerPlayer",value:p})}>+ Register player</button><button className="primary" onClick={()=>goInvoices(p.id)}>Invoice Group</button></div>{next&&<button className="rollover-link" onClick={()=>open({kind:"termRollover",value:{currentTermId:termId,nextTermId:next.id,programmeId:p.id}})}>Review Next Term →</button>}</article>}

function Coaches({data,open}:{data:Snapshot;open:(v:{kind:string;value?:unknown})=>void}){return <div className="page"><div className="page-intro"><div><h2>Coaches</h2><p>Contact details, compliance, pay and current assignments in one place.</p></div><button className="primary" onClick={()=>open({kind:"coach"})}>+ Add coach</button></div><div className="coach-grid">{data.staff.map(coach=>{const programmes=data.programmes.filter(p=>p.coach===coach.name),sessions=data.sessions.filter(s=>s.coach===coach.name);return <article className="panel coach-card" key={coach.id}><div className="coach-head"><span className="profile-avatar">{coach.name.split(" ").map(x=>x[0]).join("")}</span><div><h3>{coach.name}</h3><p>{coach.role} · {coach.employmentStatus||"Contractor"}</p></div><span className={`traffic ${coach.status==="Active"?"green":"amber"}`}>{coach.status}</span></div><dl><div><dt>Contact</dt><dd>{coach.email||"Email not added"}<br/>{coach.phone||"Phone not added"}</dd></div><div><dt>Qualifications</dt><dd>{coach.qualifications||"Not recorded"}</dd></div><div><dt>Safeguarding</dt><dd>{coach.safeguarding||"Needs recording"}</dd></div><div><dt>Pay rate</dt><dd>{money(coach.payRatePence||0)} per hour/session</dd></div><div><dt>Assignments</dt><dd>{programmes.length} programmes · {sessions.length} sessions</dd></div></dl><button className="secondary full" onClick={()=>open({kind:"coach",value:coach})}>Edit coach</button></article>})}</div></div>}

function Venues({data,go}:{data:Snapshot;go:(v:View)=>void}){return <div className="page"><div className="page-intro"><div><h2>Venues</h2><p>See every location and the work operating there.</p></div></div><div className="venue-overview-grid">{data.venues.map(v=>{const programmes=data.programmes.filter(p=>p.venue===v.name),sessions=data.sessions.filter(s=>s.venue===v.name);return <button className="panel venue-overview" key={v.id} onClick={()=>go("programmes")}><span className="venue-pin">⌖</span><span><strong>{v.name}</strong><small>{v.courts} courts · {programmes.length} programmes · {sessions.length} sessions</small></span><b>›</b></button>})}</div></div>}

function Reports({data,termId}:{data:Snapshot;termId:string}){
  const [generated,setGenerated]=useState(false);
  const term=data.terms.find(row=>row.id===termId),current=data.enrolments.filter(row=>row.termId===termId&&row.status!=="Inactive"),active=current.filter(row=>row.status==="Active"),trials=current.filter(row=>row.status==="Trial");
  const adultTypes=new Set(["Adult course","Pay & Play","Cardio Tennis"]),juniorProgrammeIds=new Set(data.programmes.filter(row=>!adultTypes.has(row.type||"Junior course")&&row.type!=="Private lesson").map(row=>row.id)),adultProgrammeIds=new Set(data.programmes.filter(row=>adultTypes.has(row.type||"")).map(row=>row.id));
  const juniorCount=active.filter(row=>juniorProgrammeIds.has(row.programmeId)).length,adultCount=active.filter(row=>adultProgrammeIds.has(row.programmeId)).length,privateLessons=data.sessions.filter(row=>row.termId===termId&&row.title.toLowerCase().includes("private")).length;
  const joinedThisTerm=(customerId:string)=>{const customer=data.customers.find(value=>value.id===customerId);return Boolean(customer&&term&&customer.createdAt.slice(0,10)>=term.startDate&&customer.createdAt.slice(0,10)<=term.endDate)};
  const newJoiners=active.filter(row=>joinedThisTerm(row.customerId)).length;
  const termInvoiceIds=new Set(data.invoices.filter(invoice=>invoice.termId===termId).map(invoice=>invoice.id)),termCustomerIds=new Set(current.map(row=>row.customerId));
  const termPayments=data.payments.filter(payment=>payment.invoiceId?termInvoiceIds.has(payment.invoiceId):termCustomerIds.has(payment.customerId));
  const received=termPayments.reduce((total,payment)=>total+payment.amountPence,0),outstanding=data.invoices.filter(invoice=>invoice.termId===termId&&invoice.lifecycleStatus==="Active").reduce((total,invoice)=>total+balanceForInvoice(data,invoice),0);
  const completeRegisters=data.programmeRegisters.filter(register=>register.termId===termId&&register.status==="Complete"),registerIds=new Set(completeRegisters.map(register=>register.id)),marks=data.programmeRegisterMarks.filter(mark=>registerIds.has(mark.registerId)),attendanceRate=marks.length?Math.round(marks.filter(mark=>["Present","Late"].includes(mark.mark)).length/marks.length*100):0;
  const programmeRows=data.programmes.map(programme=>{const registrations=current.filter(row=>row.programmeId===programme.id),activeRows=registrations.filter(row=>row.status==="Active"),trialRows=registrations.filter(row=>row.status==="Trial"),registers=completeRegisters.filter(row=>row.programmeId===programme.id),ids=new Set(registers.map(row=>row.id)),programmeMarks=marks.filter(mark=>ids.has(mark.registerId)),rate=programmeMarks.length?Math.round(programmeMarks.filter(mark=>["Present","Late"].includes(mark.mark)).length/programmeMarks.length*100):0;return {programme,active:activeRows.length,trials:trialRows.length,capacity:programme.capacity?Math.round(activeRows.length/programme.capacity*100):0,registers:registers.length,rate,newJoiners:activeRows.filter(row=>joinedThisTerm(row.customerId)).length}});
  const report={term,current,active,trials,adultProgrammeIds,juniorProgrammeIds,juniorCount,adultCount,privateLessons,newJoiners,received,outstanding,completeRegisters,marks,attendanceRate,programmeRows};
  if(generated)return <CommitteeReportDocument data={data} report={report} close={()=>setGenerated(false)}/>;
  return <div className="page committee-report"><div className="page-intro"><div><p className="section-kicker">Committee-ready insight</p><h2>{term?.name??"Current term"} performance</h2><p>Review the live figures, then generate a presentation-ready report with findings and next actions.</p></div><button className="primary generate-report-button" onClick={()=>setGenerated(true)}><AppIcon name="report"/>Generate report</button></div><section className="report-grid committee-metrics"><Metric icon="J" tone="blue" label="Active juniors" value={String(juniorCount)} note={`${trials.filter(row=>juniorProgrammeIds.has(row.programmeId)).length} junior trials`}/><Metric icon="A" tone="purple" label="Active adults" value={String(adultCount)} note={`${trials.filter(row=>adultProgrammeIds.has(row.programmeId)).length} adult trials`}/><Metric icon="+" tone="green" label="New joiners" value={String(newJoiners)} note={`Joined during ${term?.name??"this term"}`}/><Metric icon="✓" tone="yellow" label="Attendance" value={marks.length?`${attendanceRate}%`:"Not started"} note={`${completeRegisters.length} weekly registers complete`}/><Metric icon="G" tone="blue" label="Group programmes" value={String(data.programmes.filter(row=>row.type!=="Private lesson").length)} note={`${active.length} active group places`}/><Metric icon="1" tone="purple" label="Private lessons" value={String(privateLessons)} note="Scheduled in this term"/><Metric icon="£" tone="green" label="Payments recorded" value={money(received)} note={`${termPayments.length} term payment records`}/><Metric icon="!" tone="red" label="Outstanding" value={money(outstanding)} note="Across active term invoices"/></section><section className="panel report-table-card"><div className="panel-head"><div><span className="section-kicker">Group breakdown</span><h3>Programme health</h3></div><span className="traffic blue">Adults and juniors shown separately</span></div><div className="committee-table"><div className="committee-row head"><span>Programme</span><span>Category</span><span>Active</span><span>Trials</span><span>Capacity</span><span>New</span><span>Attendance</span></div>{programmeRows.map(row=><div className="committee-row" key={row.programme.id}><span><strong>{row.programme.name}</strong><small>{row.programme.venue} · {row.programme.coach}</small></span><span>{adultProgrammeIds.has(row.programme.id)?"Adult":row.programme.type==="Private lesson"?"Private":"Junior"}</span><span><strong>{row.active}</strong></span><span>{row.trials}</span><span><strong>{row.capacity}%</strong><small>{row.active} of {row.programme.capacity}</small></span><span>{row.newJoiners}</span><span><em className={`traffic ${row.registers?row.rate>=75?"green":"amber":"blue"}`}>{row.registers?`${row.rate}%`:"No registers"}</em></span></div>)}</div></section><section className="panel report-venues"><div className="report-section-title"><span className="section-kicker">Venue summary</span><h3>Activity by location</h3></div>{data.venues.map(venue=>{const programmes=data.programmes.filter(programme=>programme.venue===venue.name),ids=new Set(programmes.map(programme=>programme.id)),registrations=current.filter(row=>ids.has(row.programmeId));return <div key={venue.id}><strong>{venue.name}</strong><span>{programmes.length} programmes</span><span>{registrations.filter(row=>row.status==="Active").length} active players</span><span>{registrations.filter(row=>row.status==="Trial").length} trials</span></div>})}</section></div>
}

type CommitteeReportData={term:Term|undefined;current:Enrolment[];active:Enrolment[];trials:Enrolment[];adultProgrammeIds:Set<string>;juniorProgrammeIds:Set<string>;juniorCount:number;adultCount:number;privateLessons:number;newJoiners:number;received:number;outstanding:number;completeRegisters:ProgrammeRegister[];marks:ProgrammeRegisterMark[];attendanceRate:number;programmeRows:{programme:Programme;active:number;trials:number;capacity:number;registers:number;rate:number;newJoiners:number}[]};

function CommitteeReportDocument({data,report,close}:{data:Snapshot;report:CommitteeReportData;close:()=>void}){
  const business=data.settings.find(row=>row.key==="business_name")?.value||"Tennis business",termName=report.term?.name||"Current term",totalPlaces=data.programmes.reduce((sum,programme)=>sum+programme.capacity,0),used=report.active.length+report.trials.length,utilisation=totalPlaces?Math.round(report.active.length/totalPlaces*100):0;
  const strongest=[...report.programmeRows].filter(row=>row.active||row.trials).sort((a,b)=>b.capacity-a.capacity)[0],needsAttention=report.programmeRows.filter(row=>row.capacity<40||(!row.registers&&row.active>0)||row.rate&&row.rate<70);
  const juniorTrials=report.trials.filter(row=>report.juniorProgrammeIds.has(row.programmeId)).length,adultTrials=report.trials.filter(row=>report.adultProgrammeIds.has(row.programmeId)).length;
  const headline=report.active.length?`${report.active.length} active group places are recorded across ${data.programmes.length} programmes, with ${report.newJoiners} new joiner${report.newJoiners===1?"":"s"} during ${termName}.`:`No active group places are recorded for ${termName}.`;
  const attendanceFinding=report.marks.length?`Recorded attendance is ${report.attendanceRate}% across ${report.completeRegisters.length} completed weekly registers.`:"Attendance reporting cannot yet be assessed because no weekly registers have been completed.";
  const actions=[
    report.trials.length?`Review ${report.trials.length} trial player${report.trials.length===1?"":"s"} and record whether each is joining, extending or leaving.`:"Continue using the trial workflow to measure conversion into active players.",
    needsAttention.length?`Review ${needsAttention.length} programme${needsAttention.length===1?"":"s"} with low capacity use, incomplete registers or attendance below 70%.`:"Maintain current programme delivery and monitor capacity and attendance weekly.",
    report.outstanding?`Follow up ${money(report.outstanding)} of outstanding term invoices and record payments against the correct invoice.`:"No outstanding term invoice balance requires action.",
    report.completeRegisters.length<Math.max(1,data.programmes.filter(row=>row.type!=="Private lesson").length)?"Complete weekly registers consistently so future reports provide stronger attendance evidence.":"Keep weekly registers complete so committee reporting remains evidence-based."
  ];
  return <div className="page generated-report-page"><div className="report-action-bar no-print"><button className="secondary" onClick={close}>← Back to statistics</button><div><button className="secondary" onClick={()=>window.print()}>Print / Save PDF</button></div></div><article className="generated-report"><header className="report-cover"><div><span className="report-wordmark">TG</span><span><strong>Tennis Growth</strong><small>Committee performance report</small></span></div><span className="traffic green">Generated from live records</span><h2>{business}</h2><h1>{termName} Performance Report</h1><p>Prepared for committee and coaching-team review</p><dl><div><dt>Term</dt><dd>{report.term?`${new Date(`${report.term.startDate}T12:00:00Z`).toLocaleDateString("en-GB")} to ${new Date(`${report.term.endDate}T12:00:00Z`).toLocaleDateString("en-GB")}`:"Dates not recorded"}</dd></div><div><dt>Generated</dt><dd>{new Date().toLocaleDateString("en-GB",{day:"numeric",month:"long",year:"numeric"})}</dd></div></dl></header><section className="report-narrative"><span className="report-number">01</span><div><p className="section-kicker">Executive summary</p><h3>What the committee needs to know</h3><p>{headline} Juniors account for {report.juniorCount} active places and adults account for {report.adultCount}. {attendanceFinding}</p><div className="report-highlights"><span><small>Active places</small><strong>{report.active.length}</strong></span><span><small>New joiners</small><strong>{report.newJoiners}</strong></span><span><small>Capacity used</small><strong>{utilisation}%</strong></span><span><small>Outstanding</small><strong>{money(report.outstanding)}</strong></span></div></div></section><section className="report-narrative"><span className="report-number">02</span><div><p className="section-kicker">Participation</p><h3>Junior and adult programme position</h3><div className="participation-grid"><article><span className="category-icon junior">J</span><div><strong>{report.juniorCount} active juniors</strong><p>{juniorTrials} junior trial{juniorTrials===1?"":"s"} currently recorded. Junior numbers include active enrolments in junior course and performance programmes.</p></div></article><article><span className="category-icon adult">A</span><div><strong>{report.adultCount} active adults</strong><p>{adultTrials} adult trial{adultTrials===1?"":"s"} currently recorded across adult courses, Pay & Play and Cardio Tennis.</p></div></article></div></div></section><section className="report-narrative report-breakdown-section"><span className="report-number">03</span><div><p className="section-kicker">Programme breakdown</p><h3>How each group is performing</h3><div className="generated-report-table"><div className="generated-report-row head"><span>Programme</span><span>Type</span><span>Players</span><span>Capacity</span><span>New</span><span>Attendance</span></div>{report.programmeRows.map(row=><div className="generated-report-row" key={row.programme.id}><span><strong>{row.programme.name}</strong><small>{row.programme.venue}<br/>{row.programme.coach}</small></span><span>{report.adultProgrammeIds.has(row.programme.id)?"Adult":row.programme.type==="Private lesson"?"Private":"Junior"}</span><span><strong>{row.active}</strong><small>{row.trials} trial{row.trials===1?"":"s"}</small></span><span><strong>{row.capacity}%</strong><small>{row.active} of {row.programme.capacity}</small></span><span>{row.newJoiners}</span><span>{row.registers?`${row.rate}%`:`Not recorded`}</span></div>)}</div>{strongest&&<p className="report-callout"><strong>Strongest capacity position:</strong> {strongest.programme.name} is using {strongest.capacity}% of active capacity, with {strongest.active} active player{strongest.active===1?"":"s"}.</p>}</div></section><section className="report-narrative"><span className="report-number">04</span><div><p className="section-kicker">Attendance and delivery</p><h3>Evidence from weekly registers</h3><p>{attendanceFinding} {report.completeRegisters.length?"Present and late marks are counted as attended.":"Coaches should complete each weekly register before attendance trends are used for decisions."}</p><div className="report-highlights three"><span><small>Completed registers</small><strong>{report.completeRegisters.length}</strong></span><span><small>Attendance marks</small><strong>{report.marks.length}</strong></span><span><small>Private lessons</small><strong>{report.privateLessons}</strong></span></div></div></section><section className="report-narrative"><span className="report-number">05</span><div><p className="section-kicker">Financial position</p><h3>Invoices and payments</h3><p>{money(report.received)} has been recorded against customers or invoices associated with {termName}. The current outstanding balance on active term invoices is {money(report.outstanding)}.</p><div className="report-highlights two"><span><small>Payments recorded</small><strong>{money(report.received)}</strong></span><span className={report.outstanding?"attention":"healthy"}><small>Outstanding balance</small><strong>{money(report.outstanding)}</strong></span></div></div></section><section className="report-narrative report-actions-section"><span className="report-number">06</span><div><p className="section-kicker">Recommended next actions</p><h3>What should happen next</h3><ol>{actions.map((action,index)=><li key={action}><span>{index+1}</span><p>{action}</p></li>)}</ol></div></section><footer className="generated-report-footer"><strong>{business}</strong><span>{termName} · Tennis Growth Admin</span><small>This report reflects saved system records at the time it was generated. Missing or incomplete registers will affect attendance analysis.</small></footer></article></div>
}

function CoachForm({coach,close,save}:{coach?:Staff;close:()=>void;save:(s:Staff)=>void}){const [s,setS]=useState<Staff>(coach??{id:`coach-${Date.now()}`,name:"",role:"Coach",email:"",phone:"",qualifications:"",safeguarding:"",employmentStatus:"Contractor",payRatePence:0,notes:"",status:"Active"});const field=(key:keyof Staff,value:string|number)=>setS({...s,[key]:value});return <Modal title={coach?`Edit ${coach.name}`:"Add coach"} kicker="Coach record" close={close}><form className="form-grid" onSubmit={e=>{e.preventDefault();save(s)}}><Field label="Name"><input required value={s.name} onChange={e=>field("name",e.target.value)}/></Field><Field label="Role"><input value={s.role} onChange={e=>field("role",e.target.value)}/></Field><Field label="Email"><input type="email" value={s.email} onChange={e=>field("email",e.target.value)}/></Field><Field label="Telephone"><input value={s.phone} onChange={e=>field("phone",e.target.value)}/></Field><Field label="Qualifications"><input value={s.qualifications} onChange={e=>field("qualifications",e.target.value)}/></Field><Field label="Employment status"><select value={s.employmentStatus} onChange={e=>field("employmentStatus",e.target.value)}><option>Contractor</option><option>Employee</option><option>Volunteer</option></select></Field><Field label="Hourly or session rate"><input type="number" value={s.payRatePence/100} onChange={e=>field("payRatePence",Math.round(+e.target.value*100))}/></Field><Field label="Status"><select value={s.status} onChange={e=>field("status",e.target.value)}><option>Active</option><option>Inactive</option></select></Field><label className="wide">DBS and safeguarding<textarea value={s.safeguarding} onChange={e=>field("safeguarding",e.target.value)}/></label><label className="wide">Notes<textarea value={s.notes} onChange={e=>field("notes",e.target.value)}/></label><Actions close={close} label={coach?"Save coach":"Add coach"}/></form></Modal>}

function ProgrammeEditor({programme,data,close,save}:{programme:Programme;data:Snapshot;close:()=>void;save:(p:Programme)=>void}){const [p,setP]=useState({...programme,type:programme.type||"Junior course",minAgeYears:programme.minAgeYears??0,maxAgeYears:programme.maxAgeYears??99,suggestedNextProgrammeId:programme.suggestedNextProgrammeId||""});const field=(key:keyof Programme,value:string|number)=>setP({...p,[key]:value});return <Modal title={`Edit ${programme.name}`} kicker="Programme details" close={close}><form className="form-grid" onSubmit={e=>{e.preventDefault();save(p)}}><Field label="Programme name"><input value={p.name} onChange={e=>field("name",e.target.value)}/></Field><Field label="Programme type"><select value={p.type} onChange={e=>field("type",e.target.value)}>{["Junior course","Adult course","Pay & Play","Private lesson","Holiday camp","Cardio Tennis","Other"].map(type=><option key={type}>{type}</option>)}</select></Field><Field label="Venue"><select value={p.venue} onChange={e=>field("venue",e.target.value)}>{data.venues.map(v=><option key={v.id}>{v.name}</option>)}</select></Field><Field label="Coach"><select value={p.coach} onChange={e=>field("coach",e.target.value)}><option>Unassigned</option>{data.staff.filter(s=>s.status==="Active").map(s=><option key={s.id}>{s.name}</option>)}</select></Field><Field label="Day"><select value={p.weekday} onChange={e=>field("weekday",+e.target.value)}>{["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"].map((day,index)=><option value={index} key={day}>{day}</option>)}</select></Field><Field label="Start time"><input type="time" value={p.startTime} onChange={e=>field("startTime",e.target.value)}/></Field><Field label="Capacity"><input type="number" value={p.capacity} onChange={e=>field("capacity",+e.target.value)}/></Field><Field label="Price per session"><input type="number" value={p.pricePence/100} onChange={e=>field("pricePence",Math.round(+e.target.value*100))}/></Field>{p.type==="Junior course"&&<><Field label="Recommended minimum age"><input type="number" min="0" value={p.minAgeYears} onChange={e=>field("minAgeYears",+e.target.value)}/></Field><Field label="Recommended maximum age"><input type="number" min="0" value={p.maxAgeYears} onChange={e=>field("maxAgeYears",+e.target.value)}/></Field><Field label="Suggested next programme"><select value={p.suggestedNextProgrammeId} onChange={e=>field("suggestedNextProgrammeId",e.target.value)}><option value="">No automatic suggestion</option>{data.programmes.filter(row=>row.id!==p.id).map(row=><option key={row.id} value={row.id}>{row.name} · {row.venue}</option>)}</select></Field></>}<Actions close={close} label="Save programme"/></form></Modal>}

function InvoiceSendReview({invoices,data,gmail,close,send}:{invoices:Invoice[];data:Snapshot;gmail:GmailConnection|null;close:()=>void;send:(ids:number[])=>void}){const [selected,setSelected]=useState(invoices.map(i=>i.id));const mode=gmail?.deliveryMode||"test";return <Modal title={`Ready to send ${selected.length} invoice email${selected.length===1?"":"s"}`} kicker={mode==="test"?"TEST MODE · Rob only":"LIVE MODE · Parent delivery"} close={close}><div className={`delivery-warning ${mode}`}><strong>{mode==="test"?"TEST MODE":"LIVE MODE"}</strong><span>From: {gmail?.email||"Connected Gmail"}</span><span>{mode==="test"?`Actual recipient: ${gmail?.testRecipient||"robnorris125@gmail.com"}`:"Each message goes separately to the saved customer address shown below."}</span></div><div className="send-review-list">{invoices.map(invoice=>{const customer=data.customers.find(c=>c.id===invoice.customerId),programme=data.programmes.find(p=>p.id===invoice.programmeId),term=data.terms.find(t=>t.id===invoice.termId);return <label key={invoice.id}><input type="checkbox" checked={selected.includes(invoice.id)} onChange={()=>setSelected(selected.includes(invoice.id)?selected.filter(id=>id!==invoice.id):[...selected,invoice.id])}/><span><strong>{customer?.player}</strong><small>Parent: {customer?.payer}</small><small>{mode==="test"?"Intended recipient":"To"}: {customer?.email}</small><small>{programme?.name} · {term?.name} · {invoice.invoiceNumber}</small></span><span className="review-amount"><strong>{money(invoice.amountPence)}</strong><small>✓ Matching PDF attached</small></span></label>})}</div><div className="privacy-confirm">Each family receives a separate private message. No recipient can see another family’s details.</div><div className="modal-actions"><button className="secondary" onClick={close}>Cancel</button><button className="primary" disabled={!selected.length} onClick={()=>send(selected)}>{mode==="test"?`Send ${selected.length} Test Email${selected.length===1?"":"s"} & PDFs`:`Send ${selected.length} Live Email${selected.length===1?"":"s"} & PDFs`}</button></div></Modal>}

function OperationalSchedule({data,termId,add,register,substitute}:{data:Snapshot;termId:string;add:()=>void;register:(s:Session)=>void;substitute:(s:Session)=>void}){const [venue,setVenue]=useState("All"),[coach,setCoach]=useState("All");const rows=data.sessions.filter(s=>s.termId===termId&&(venue==="All"||s.venue===venue)&&(coach==="All"||s.coach===coach));const venueNames=venue==="All"?data.venues.map(v=>v.name):[venue];return <div className="page"><div className="page-intro"><div><h2>Schedule by venue</h2><p>Open a register or change the coach for one individual session.</p></div><button className="primary" onClick={add}>+ Add session</button></div><div className="schedule-filter panel"><label>Venue<select value={venue} onChange={e=>setVenue(e.target.value)}><option>All</option>{data.venues.map(v=><option key={v.id}>{v.name}</option>)}</select></label><label>Coach<select value={coach} onChange={e=>setCoach(e.target.value)}><option>All</option>{data.staff.map(s=><option key={s.id}>{s.name}</option>)}</select></label><span>{rows.length} session{rows.length===1?"":"s"} shown</span></div><div className="schedule-venues">{venueNames.map(venueName=>{const venueRows=rows.filter(s=>s.venue===venueName);if(!venueRows.length)return null;return <section className="panel schedule-venue" key={venueName}><header><span className="venue-pin">⌖</span><div><h3>{venueName}</h3><p>{venueRows.length} scheduled session{venueRows.length===1?"":"s"}</p></div></header>{venueRows.map(session=>{const completed=data.attendance.some(a=>a.sessionId===session.id),missing=!session.coach||session.coach==="Unassigned";return <article className="schedule-session" key={session.id}><time><strong>{["Mon","Tue","Wed","Thu","Fri","Sat","Sun"][session.day]}</strong><span>{session.start}</span></time><div><h4>{session.title}</h4><p>{session.duration} minutes · {session.meta}</p></div><div><small>Coach</small><strong className={missing?"text-red":""}>{missing?"Coach required":session.coach}</strong><em className={`traffic ${completed?"green":"amber"}`}>{completed?"Register complete":"Register incomplete"}</em></div><div className="schedule-actions"><button className="secondary" onClick={()=>substitute(session)}>Change coach</button><button className="primary" onClick={()=>register(session)}>Open register</button></div></article>})}</section>})}</div></div>}

function SubstituteCoachForm({session,staff,close,save}:{session:Session;staff:Staff[];close:()=>void;save:(coach:string)=>void}){const [coach,setCoach]=useState(session.coach||staff[0]?.name||"Unassigned");return <Modal title="Assign cover coach" kicker="This session only" close={close}><p className="modal-copy"><strong>{session.title}</strong><br/>{session.sessionDate} at {session.start}, {session.venue}. The permanent programme coach will not change.</p><form className="form-grid" onSubmit={e=>{e.preventDefault();save(coach)}}><Field label="Coach for this session"><select value={coach} onChange={e=>setCoach(e.target.value)}><option>Unassigned</option>{staff.map(s=><option key={s.id}>{s.name}</option>)}</select></Field><div className="registration-note wide"><strong>Temporary substitution</strong><span>Only this dated session is updated. Future sessions keep their normal coach.</span></div><Actions close={close} label="Assign coach"/></form></Modal>}

function SettingsHub({data,calendar,mutate,flash}:{data:Snapshot;calendar:boolean;mutate:(a:string,p?:unknown)=>Promise<Snapshot>;flash:(m:string)=>void}){const get=(key:string,fallback="")=>data.settings.find(s=>s.key===key)?.value||fallback;const [values,setValues]=useState({business_name:get("business_name","Supreme Tennis"),business_address:get("business_address"),business_phone:get("business_phone"),business_email:get("business_email","robnorris125@gmail.com"),business_website:get("business_website"),primary_colour:get("primary_colour","#1f63e9"),secondary_colour:get("secondary_colour","#ffe735"),bank_details:get("bank_details"),invoice_footer:get("invoice_footer","Thank you for your payment."),email_signature:get("email_signature","Kind regards,\nSupreme Tennis")}),[saving,setSaving]=useState(false);const field=(key:keyof typeof values,value:string)=>setValues({...values,[key]:value});const save=async()=>{if(saving)return;setSaving(true);try{await mutate("setSettings",{values});flash("Business branding and contact details saved")}catch(error){flash(error instanceof Error?error.message:"Business settings could not be saved")}finally{setSaving(false)}};const toggle=async()=>{await mutate("setSetting",{key:"calendar_test_connected",value:String(!calendar)});flash(!calendar?"Calendar test connection saved":"Calendar test disconnected")};return <div className="page"><div className="page-intro"><div><h2>Business settings</h2><p>Set the details that will be used across invoices, emails and future receipts.</p></div></div><section className="branding-layout"><form className="panel branding-form" onSubmit={e=>{e.preventDefault();void save()}}><div className="panel-head"><div><span className="section-kicker">Business branding</span><h3>Your customer-facing details</h3></div><span className="traffic blue">Saved to this business</span></div><div className="form-grid"><Field label="Business name"><input value={values.business_name} onChange={e=>field("business_name",e.target.value)}/></Field><Field label="Business email"><input type="email" value={values.business_email} onChange={e=>field("business_email",e.target.value)}/></Field><Field label="Telephone"><input value={values.business_phone} onChange={e=>field("business_phone",e.target.value)}/></Field><Field label="Website"><input value={values.business_website} onChange={e=>field("business_website",e.target.value)}/></Field><label className="wide">Business address<textarea value={values.business_address} onChange={e=>field("business_address",e.target.value)}/></label><Field label="Primary colour"><input type="color" value={values.primary_colour} onChange={e=>field("primary_colour",e.target.value)}/></Field><Field label="Secondary colour"><input type="color" value={values.secondary_colour} onChange={e=>field("secondary_colour",e.target.value)}/></Field><label className="wide">Bank or payment information<textarea value={values.bank_details} onChange={e=>field("bank_details",e.target.value)}/></label><label className="wide">Invoice footer<textarea value={values.invoice_footer} onChange={e=>field("invoice_footer",e.target.value)}/></label><label className="wide">Email signature<textarea value={values.email_signature} onChange={e=>field("email_signature",e.target.value)}/></label><div className="modal-actions wide"><button className="primary" disabled={saving}>{saving?"Saving…":"Save business settings"}</button></div></div></form><aside className="settings-side"><article className="panel brand-preview"><span className="section-kicker">Preview</span><div className="brand-preview-logo" style={{background:values.primary_colour,color:values.secondary_colour}}>{values.business_name.slice(0,1)||"S"}</div><h3>{values.business_name||"Business name"}</h3><p>{values.business_email}<br/>{values.business_phone}<br/>{values.business_website}</p><div className="brand-swatch"><i style={{background:values.primary_colour}}/><i style={{background:values.secondary_colour}}/></div></article><article className="panel integration-card"><div className="integration-head"><span className="google-g hero">G</span><div><p className="section-kicker">Calendar test</p><h3>{calendar?"Test connection active":"Not connected"}</h3></div><span className={`traffic ${calendar?"green":"amber"}`}>{calendar?"Saved":"Off"}</span></div><p>Calendar testing remains separate from financial records.</p><button className="secondary full" onClick={toggle}>{calendar?"Disconnect test calendar":"Connect test calendar"}</button></article></aside></section></div>}
